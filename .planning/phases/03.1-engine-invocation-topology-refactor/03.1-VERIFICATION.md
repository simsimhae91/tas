---
phase: 03.1-engine-invocation-topology-refactor
verified: 2026-04-21T00:00:00Z
status: human_needed
score: 6/7 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Run /tas end-to-end with the Scenario B topology — confirm dialectic first round completes without premature engine termination"
    expected: "Single step in a fresh /tas invocation survives the 8-12 min first-round LLM roundtrip; MetaAgent polling observes engine.done + engine.exit; final JSON (status: completed | halted) returned to MainOrchestrator; no subagent-orphan-reap hang observed"
    why_human: "TOPO-06 success requires real Claude Code harness + real dialectic LLM roundtrip. Synthetic Canary #7 Phase 1 + Phase 2 cover topology invariants (orphan survival + marker-writing trap) but the real Agent() round-trip + real Thesis/Antithesis SDK session are not reproducible in CI. Plan 07 SUMMARY explicitly flagged this as manual-verification territory and REQUIREMENTS.md TOPO-06 notes '사후 검증' (post-verification)."
gaps: []
---

# Phase 03.1: Engine Invocation Topology Refactor Verification Report

**Phase Goal:** MetaAgent 서브에이전트가 `nohup bash run-dialectic.sh ... & echo $!` fire-and-forget 으로 엔진을 기동하고, `run-dialectic.sh` EXIT trap 이 `engine.done` + `engine.exit` atomic marker 를 생성하며, MetaAgent 가 `test -f engine.done || ! kill -0 $PID` 폴링 루프로 완료를 관측해 Claude Code harness 의 subagent orphan reap 으로 인한 dialectic 조기 종료를 구조적으로 제거한다. Canary #7 (subagent orphan survival 자동화) 추가로 회귀 방지. 신규 halt_reason enum 금지 · 4계층 info hiding 보존 · stdlib-only.

**Note on goal wording:** The phase goal statement above uses the original Scenario A framing ("SKILL.md Phase 2 가 ... 폴링 루프로"). During plan review, Issue #2 pivoted the project to **Scenario B**: MetaAgent owns polling internally within a single Agent() call; SKILL.md consumes only the pre-existing `status: completed | halted` JSON envelope. This pivot is locked in 03.1-CONTEXT.md §D-TOPO-04 and PLAN-REVIEW-phase-3.1.md. Verification evaluates TOPO-03 against Scenario B semantics.

**Verified:** 2026-04-21 UTC
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (derived from phase goal + ROADMAP §Phase 3.1 + REQUIREMENTS §C.1)

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1   | MetaAgent spawns engine via `nohup bash run-dialectic.sh ... & echo $!` with `run_in_background: false` (fire-and-forget contract) | VERIFIED | meta.md L494-502 contains the exact nohup Bash spawn pattern with `run_in_background: false`; `run_in_background: true` count in meta.md = 0; `bash_id`/`output_file_path` count = 0. TOPO-02 contract present. |
| 2   | `run-dialectic.sh` EXIT trap writes atomic `engine.done` + `engine.exit` markers on every exit path; `exec` keyword absent (Issue #1 fix) | VERIFIED | run-dialectic.sh L35-46 contains the TOPO-01 trap block writing both markers via `mv -f` atomic rename; `grep -c "^[[:space:]]*exec "` == 0 (live check); `bash -n` OK; test_exit_trap.sh 5-case passes (Test A/B/D/E PASS, C SKIP on macOS without coreutils) |
| 3   | MetaAgent polls engine completion locally (`test -f engine.done` OR `! kill -0 $PID`) in ≤9.5-min chunks (Scenario B) | VERIFIED | meta.md L526-558 contains step 8 with `for i in $(seq 1 19); do test -f engine.done ... kill -0 $ENGINE_PID ... sleep $TAS_POLL_INTERVAL_SEC ...` chunked polling pattern; `TAS_POLL_INTERVAL_SEC` env override present; `engine_launched` / `Return early with engine metadata` / `task-notification` residue counts all 0. Scenario B ownership confirmed in meta.md L526 "MetaAgent-owned". |
| 4   | No new halt_reason enums introduced; `step_transition_hang` absorbs the polling-orphan-death path (D-TOPO-05) | VERIFIED | Cross-file enum-setting-pattern grep across 5 files (SKILL.md + meta.md + dialectic.py + checkpoint.py + engine-invocation-protocol.md) = 0 matches; mention-pattern matches only appear in SKILL.md L308 prohibition bullet (intentional guardrail). meta.md step 8b classification uses existing 3-enum set + engine_crash only. |
| 5   | 4-layer info hiding preserved — SKILL.md Allowed Read list unchanged; info-hiding 9-match baseline held | VERIFIED | `grep -cE "(dialogue\\.md\|round-.*\\.md\|deliverable\\.md\|lessons\\.md\|heartbeat\\.txt)" skills/tas/SKILL.md` = 9 (pre-Phase-3.1 baseline preserved). SKILL.md diff is +1 line (Phase 0b halt_reason freeze bullet, Plan 05); Phase 2 handler byte-identical. heartbeat.txt cat stays inside meta.md step 8b (L596-602). engine-invocation-protocol.md "What NOT to do" lists SKILL.md artifact-read prohibition. |
| 6   | Canary #7 registered in /tas-verify with Phase 1 (orphan survival) + Phase 2 (real-chain integration) — regression guard for TOPO-01..06 + Issue #1 | VERIFIED | simulate_subagent_orphan.py (291 lines, stdlib-only) implements both Phases. Smoke mode (T=10) executed live: `PASS: canary #7 (subagent orphan survived 10s, PPID=1; real-chain integration: PASS)`. canaries.md §"Canary #7 — Subagent-spawned orphan survival" registered with Phase 1 + Phase 2 Pass criteria + Fail signals. simulate_subagent_orphan.sh delegates via exec python3. |
| 7   | End-to-end /tas run completes dialectic first round with Scenario B topology (TOPO-06) | UNCERTAIN | Canary #7 Phase 2 exercises the real `run-dialectic.sh` via sed-copy mock injection (no LLM), confirming the EXIT trap fires in the production code path. However, the full Agent() round-trip with real Thesis/Antithesis SDK sessions requires a real `/tas` invocation and is explicitly flagged as manual verification in VALIDATION.md §"Manual-Only Verifications" and REQUIREMENTS.md §C.1 TOPO-06 comment ("사후 검증"). |

**Score:** 6/7 truths verified; 1 routed to human verification

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `skills/tas/runtime/run-dialectic.sh` | EXIT trap block + 0 `exec` keywords + byte-identical find_python preflight + Layer B watchdog | VERIFIED | L35-46 TOPO-01 trap block present; L68-78 Layer B branches have `$PYTHON ... dialectic.py "$@"` without `exec`; grep count 0; `bash -n` OK; L1-33 find_python preserved |
| `skills/tas/agents/meta.md` | step 7 nohup spawn + step 8/8b Scenario B polling + classify + forensic heartbeat cat; no task-notification/engine_launched/Return early | VERIFIED | L494-618 cover step 7 spawn + step 8 polling + step 8b classify; all scenario-A residue (engine_launched/bash_id/output_file_path/Return early/Await completion/task-notification) returns count 0; step 9 (Read deliverable) + step 9.5 (checkpoint) byte-identical |
| `skills/tas/references/engine-invocation-protocol.md` | Scenario B rewrite (8 sections) + 143 row in classification table + exec-forbidden bullet; task-notification purge | VERIFIED | 247 lines (≥120 required); 8 sections all present; nohup/echo $!/engine.done/engine.exit/run_in_background: false all grep-positive; task-notification count 0; 143 row present; exec forbidden bullet present |
| `skills/tas/SKILL.md` | Phase 0b prohibition block adds halt_reason enum freeze bullet; everything else byte-identical | VERIFIED | L308 contains "Introduce new halt_reason enums (engine_lost, polling_orphan_death, engine_exit_missing) — Phase 3.1 D-TOPO-05 freeze" inside HTML comment block; info-hiding baseline 9 maintained; Phase 2.5 polling literals (DONE_PATH/EXIT_PATH/TAS_POLL_INTERVAL_SEC/for i in $(seq 1 19)) absent (count 0) — Scenario B honored |
| `CLAUDE.md` | 3 Common Mistakes bullets (replace foreground + nohup load-bearing + exec forbidden); "ALWAYS use run_in_background: true" purged | VERIFIED | Phase 3.1 paradigm / Scenario B / nohup bash run-dialectic / Omitting any of nohup/&/echo / Using exec inside ... run-dialectic / simulate_subagent_orphan.py all grep-positive; ALWAYS use run_in_background: true count = 0 |
| `skills/tas/runtime/tests/simulate_subagent_orphan.py` | 2-Phase canary (orphan survival + real-chain integration); stdlib-only; smoke mode T=10 exits 0 | VERIFIED | 291 lines; no claude_agent_sdk/pytest/psutil imports; phase 1 asserts duration<10s + PPID=1 + marker='survived'; phase 2 sed-copies run-dialectic.sh with mock injection; live run returns PASS (both phases) |
| `skills/tas/runtime/tests/simulate_subagent_orphan.sh` | Thin wrapper delegating to Python harness | VERIFIED | 23 lines; header updated (no PENDING); `exec python3 $PYTHON_HARNESS` body preserved |
| `skills/tas/runtime/tests/test_exit_trap.sh` | 5 test cases (A/B/C/D/E) covering normal exit / raise / SIGTERM / atomicity / real-chain integration | VERIFIED | 172 lines; live run shows A/B/D/E PASS + C SKIP (no coreutils); regression guards Issue #1 via both Test E and static grep fallback |
| `skills/tas-verify/canaries.md` | Canary #7 registered with Phase 1 + Phase 2 Pass criteria / Fail signals | VERIFIED | §"Canary #7 — Subagent-spawned orphan survival (VERIFY-TOPO-01)" section present; Phase 1 + Phase 2 both documented; no PENDING status; Issue #1 regression signal documented; smoke T=10s and extended T=1800s modes both listed |
| `.planning/REQUIREMENTS.md` | §C.1 7 rows (TOPO-01..06 + VERIFY-TOPO-01) + 7 traceability rows | VERIFIED | 7 checkbox `[x]` rows under §C.1; 7 traceability rows "Phase 3.1 (Engine Invocation Topology Refactor) | Complete"; Coverage 35/35 updated |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| meta.md step 7 Bash call | run-dialectic.sh EXIT trap (Plan 02) | `nohup bash ${SKILL_DIR}/runtime/run-dialectic.sh ... & echo $!` pattern | WIRED | meta.md L499 exactly matches; `echo $!` on same line ensures PID capture to Bash stdout |
| run-dialectic.sh EXIT trap | `${LOG_DIR}/engine.done` + `${LOG_DIR}/engine.exit` | `trap 'rc=$?; echo "$rc" > ... .exit.tmp && mv -f ... ; : > ... .done.tmp && mv -f ...' EXIT` | WIRED | L46 single-line trap; `mv -f` atomic rename; LOG_DIR derived from $1 dirname |
| meta.md step 8 polling | `${LOG_DIR}/engine.done`, `$ENGINE_PID` | `test -f engine.done && ...; kill -0 $ENGINE_PID || ...; sleep $TAS_POLL_INTERVAL_SEC` | WIRED | L536-540 contains canonical 19×30s chunked pattern; signals done/dead/pending back to MetaAgent |
| meta.md step 8b classify | `${LOG_DIR}/engine.exit` + `${LOG_DIR}/engine.log` | `cat engine.exit; tail -n 1 engine.log; <apply D-05 table>` | WIRED | L565-590 pulls EXIT + LAST, maps via 5 classification bullets; synthesizes final `status: completed | halted` JSON byte-compatible with pre-Phase-3.1 handler |
| MetaAgent → MainOrchestrator | final JSON envelope | pre-Phase-3.1 `status: completed | halted` contract | WIRED | SKILL.md Phase 2 handler unchanged; MetaAgent synthesized JSON passes through existing render path; Scenario B ownership preserved |
| Canary #7 Phase 2 | real run-dialectic.sh chain | sed-copy mock injection in simulate_subagent_orphan.py + test_exit_trap.sh Test E | WIRED | Python harness L195-214 + bash Test E L138-141 both exercise the real script with mock dialectic; both run green in live checks |
| engine-invocation-protocol.md | downstream agent behavior (meta.md reads this file) | meta.md L282 "how to invoke run-dialectic.sh" + L494 "per references/engine-invocation-protocol.md" | WIRED | Protocol file is the single source of truth for Scenario B contract; meta.md cites it in both step 7 and step 8b classification |

### Data-Flow Trace (Level 4)

Not applicable at file-render level — this phase ships agent prompts + bash scripts + Python regression tests, not user-facing components rendering dynamic data. The "data flow" is the file-marker contract between `run-dialectic.sh` (writer) and `meta.md` polling (reader), verified via Canary #7 Phase 2 live run (engine.done + engine.exit both present after real-chain execution).

### Behavioral Spot-Checks

All specified live checks executed during verification:

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| `exec` keyword absent from run-dialectic.sh | `grep -c "^[[:space:]]*exec " skills/tas/runtime/run-dialectic.sh` | 0 | PASS |
| run-dialectic.sh syntax valid | `bash -n skills/tas/runtime/run-dialectic.sh` | exit 0 | PASS |
| EXIT trap regression + integration tests | `bash skills/tas/runtime/tests/test_exit_trap.sh` | Test A/B/D/E PASS; Test C SKIP (no coreutils); overall PASS | PASS |
| Canary #7 smoke mode (T=10) | `TAS_VERIFY_TOPO_DURATION_SEC=10 bash skills/tas/runtime/tests/simulate_subagent_orphan.sh` | `PASS: canary #7 (subagent orphan survived 10s, PPID=1; real-chain integration: PASS)` | PASS |
| Scenario B residue check — engine_launched in SKILL.md + meta.md | `grep -c engine_launched skills/tas/SKILL.md skills/tas/agents/meta.md` | 0 each | PASS |
| meta.md run_in_background: true residue | `grep -c "run_in_background: true" skills/tas/agents/meta.md` | 0 | PASS |
| meta.md bash_id/output_file_path residue | `grep -c "bash_id\|output_file_path" skills/tas/agents/meta.md` | 0 | PASS |
| halt_reason enum freeze (5 files) | `grep -Ec "halt_reason[\"']?\s*[:=]\s*[\"']?(engine_lost\|polling_orphan_death\|engine_exit_missing)" <5 files> \| awk sum` | 0 total | PASS |
| dialectic.py self-test (Phase 1 regression guard) | `python3 skills/tas/runtime/dialectic.py --self-test` | `PASS: 45/45 tests passed` | PASS |
| Canary #5 (Layer A stdout-stall) | `python3 skills/tas/runtime/tests/simulate_stdout_stall.py` | `PASS: canary #5 (Layer A sdk_session_hang)` | PASS |
| Canary #6 PART A (Layer B bash wrapper) | `bash skills/tas/runtime/tests/simulate_step_transition.sh` | SKIP (no coreutils) | PASS (explicit SKIP) |
| Canary #6 PART B (unit tests) | `python3 skills/tas/runtime/tests/simulate_step_transition_unit.py` | 16 tests OK | PASS |
| SKILL.md info-hiding 9-baseline | `grep -cE "(dialogue\|round-\|deliverable\|lessons\|heartbeat.txt)" skills/tas/SKILL.md` | 9 | PASS |
| meta.md polling presence (Scenario B smoke) | `grep -q "for i in \$(seq 1 19)" skills/tas/agents/meta.md` | exit 0 | PASS |
| engine-invocation-protocol.md task-notification purge | `grep -c "task-notification" skills/tas/references/engine-invocation-protocol.md` | 0 | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| TOPO-01 | 03.1-02 | run-dialectic.sh EXIT trap writes atomic engine.done + engine.exit markers | SATISFIED | trap block L35-46 + Issue #1 fix (exec removal) + test_exit_trap.sh 5-case live pass |
| TOPO-02 | 03.1-04 (via Plan 01 scaffolding) | MetaAgent step 7 nohup + & + echo $! spawn with run_in_background: false | SATISFIED | meta.md L494-502 exactly matches; topology note box documents why each element is load-bearing |
| TOPO-03 | 03.1-04 | MetaAgent-internal polling with 19×30s chunks (Scenario B pivot) | SATISFIED | meta.md L526-558 implements chunked polling + heartbeat forensic cat stays in MetaAgent; SKILL.md does not poll (verified via 0 polling literal grep) |
| TOPO-04 | 03.1-03 | engine-invocation-protocol.md rewrite — task-notification → file-marker polling | SATISFIED | 247 lines, 8 sections, task-notification count 0, nohup + file-marker contract; exec-forbidden bullet + 143 row both present |
| TOPO-05 | 03.1-05 | step_transition_hang absorbs PID-dead-without-marker path; no new halt_reason enum | SATISFIED | SKILL.md Phase 0b prohibition bullet added; cross-file enum-setting pattern count 0 across 5 source files; meta.md step 8b classify maps "lost" exit → step_transition_hang (no new enum) |
| TOPO-06 | 03.1-07 | End-to-end /tas executes dialectic first round successfully (post-verification) | SATISFIED (documentation) / NEEDS HUMAN (end-to-end) | CLAUDE.md Common Mistakes updated with 3 bullets (Scenario B paradigm + nohup load-bearing + exec forbidden); ALWAYS use run_in_background: true phrase purged. **End-to-end `/tas` run with real Claude Code harness + real dialectic LLM session is documented as manual verification in VALIDATION.md §Manual-Only Verifications and explicitly flagged as "사후 검증" in REQUIREMENTS.md.** |
| VERIFY-TOPO-01 | 03.1-06 | Canary #7 2-Phase regression guard registered in /tas-verify | SATISFIED | simulate_subagent_orphan.py (291 LOC stdlib-only) + .sh wrapper + canaries.md §Canary #7 Phase 1+2 Pass criteria + Fail signals; smoke T=10 live PASS |

No orphaned requirements. All 7 Phase 3.1 IDs account for across the 7 plans' frontmatter `requirements:` fields.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |

No Phase 3.1-introduced anti-patterns. Code Review §WR-01 (trap silently degrades when run-dialectic.sh invoked with empty $1) and §WR-02 (Canary #7 Phase 2 does not exercise the `nohup + &` spawn chain in combination) are pre-existing WARNING-level findings flagged in 03.1-REVIEW.md. Neither blocks TOPO-01..06 — WR-01 is a publicly-callable-script edge case (production path is safe; MetaAgent always passes step-config.json), and WR-02 is a minor coverage gap (Phase 1 covers nohup+& under synthetic payload; Phase 2 covers real run-dialectic.sh under foreground invocation; the combined case is intentionally lower-priority). Info-level findings (IN-01..05) are cosmetic (comment wording, step numbering, diagnostic-polish).

### Deferred Items

None. The only item not fully verified here (end-to-end /tas execution) is explicitly scoped as post-verification in REQUIREMENTS.md TOPO-06 and VALIDATION.md §Manual-Only, and is the subject of the human-verification item — not a deferred gap.

### Human Verification Required

#### 1. End-to-end /tas with Scenario B topology

**Test:** Run `/tas "<any simple dialectic request>"` in a fresh workspace with the Phase 3.1 topology active
**Expected:**
- MetaAgent subagent spawns engine via `nohup bash run-dialectic.sh ... & echo $!` (subagent Bash call returns in ms)
- Engine dialectic round completes (8-12 min expected) — observable via workspace logs showing round-1 thesis + antithesis exchanges
- MetaAgent local polling observes `engine.done` marker written + `engine.exit == "0"` at EXIT trap fire time
- MainOrchestrator receives `status: completed | halted` JSON (pre-Phase-3.1 contract)
- No subagent-orphan-reap mid-round termination (the original Phase 3 close blocker)
**Why human:** The real Claude Code harness Agent() round-trip + real SDK Thesis/Antithesis sessions are not reproducible in CI. Canary #7 Phase 1 covers orphan survival under synthetic payload; Phase 2 covers real `run-dialectic.sh` under a foreground mock. The combined case (real subagent + nohup+& + real LLM dialectic) requires a /tas execution session. REQUIREMENTS.md TOPO-06 labels this "사후 검증" (post-verification).

### Gaps Summary

No blocking gaps. All 7 requirement IDs (TOPO-01..06 + VERIFY-TOPO-01) map to implemented + live-verified artifacts across the 7 plans. The topology refactor structurally removes the Phase 3 close blocker (subagent-reap of `run_in_background: true` bash_id) by shifting to `nohup + &` fire-and-forget + MetaAgent-internal polling. The only remaining verification step is an end-to-end /tas smoke run to confirm the structural change actually resolves the production hang observed at `_workspace/quick/20260421_143000/`. That step is scoped as manual and is the subject of the human-verification item.

**Note on TOPO-06 completeness:** Plan 07 delivered the documentation-side TOPO-06 deliverable (CLAUDE.md Common Mistakes 3 bullets) and the 7-invariant regression shield. The requirement wording itself splits into two parts: (a) documentation side — covered by Plan 07 and verified here; (b) end-to-end dialectic execution — scoped as post-verification in both VALIDATION.md §Manual-Only Verifications and REQUIREMENTS.md §C.1. This verification report captures (a) as VERIFIED and (b) as the single human-verification item.

---

_Verified: 2026-04-21_
_Verifier: Claude (gsd-verifier)_
