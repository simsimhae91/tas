---
phase: 03.1-engine-invocation-topology-refactor
plan: 07
subsystem: project-guidance
tags: [claude-md, docs, common-mistakes, info-hiding-regression, scenario-b, topo-06, phase-close, regression-invariants]

# Dependency graph
requires:
  - phase: 03.1-02
    provides: run-dialectic.sh EXIT trap + `exec` removal (Issue #1 empirical fix) — precondition for Plan 07 invariant 6 (exec count == 0) and for Test E's real-exec-chain assertion that Common Mistakes bullet 3 references as Canary #7 Phase 2 backstop
  - phase: 03.1-03
    provides: engine-invocation-protocol.md Scenario B rewrite — Common Mistakes bullet 1 cites this doc as the canonical paradigm reference, and invariant `task-notification == 0 && run_in_background: false >= 1` ties the Common Mistakes paradigm directly to the protocol doc
  - phase: 03.1-04
    provides: meta.md step 7 nohup spawn + step 8/8b local polling blocks — invariant 7 (polling presence) positively asserts `for i in $(seq 1 19)`, `engine.done`, `engine.exit`, `TAS_POLL_INTERVAL_SEC`, `ENGINE_PID|engine_pid` all exist in meta.md; CLAUDE.md bullet 1 describes this exact paradigm
  - phase: 03.1-05
    provides: SKILL.md Phase 0b halt_reason enum freeze bullet — invariant 3 (cross-file halt_reason enum freeze) relies on SKILL.md having 0 enum set patterns; TOPO-05 guardrail in orchestrator prompt layer
  - phase: 03.1-06
    provides: Canary #7 2-Phase regression guard (simulate_subagent_orphan.py) — invariant 4d smoke mode relies on this canary, and Common Mistakes bullet 2 references it by file path as orphan-survival regression guard

provides:
  - CLAUDE.md Common Mistakes section now has 3 updated/new bullets establishing Phase 3.1 paradigm as load-bearing project rules: (1) replaced "run-dialectic.sh foreground" bullet with Scenario B nohup+& paradigm, (2) new "Omitting nohup/&/echo $!" bullet (all 3 elements load-bearing), (3) new "exec inside run-dialectic.sh" bullet (Issue #1 regression guard). Future Claude instances editing tas will see these at the project instructions layer.
  - Phase 3.1 end-to-end regression invariant set (7 invariants + Scenario A residue check + protocol smoke + REQUIREMENTS.md traceability) verified green. TOPO-06 requirement documentation-side complete — `/tas` ready for Phase 3.1 close via `/gsd-verify-work 3.1`.
  - Plan Review Issue #1 (exec vs EXIT trap) and Issue #2 (Plan 04 ↔ Plan 05 ownership contradiction) both close structurally: Issue #1 via invariant 6 static grep + CLAUDE.md bullet 3; Issue #2 via Scenario B ownership enforcement (meta.md owns polling; SKILL.md has zero polling logic).

affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Paradigm-shift documentation at project instructions layer (CLAUDE.md) — when an architectural decision reverses previously-documented best practice (here: run_in_background: true → run_in_background: false + nohup), replace the old bullet in place with a full paradigm explanation (not just a delete + insert) so future agents reading the replacement see the WHY (harness bash_id reap, FINDINGS §7 evidence chain) alongside the new rule. Adjacent anti-pattern bullets provide multiple entry points to the same guardrail (3-element nohup spawn; exec keyword ban)."
    - "Structural guardrail via positive + negative invariants — Plan 07 combines presence assertions (invariant 7: meta.md polling block must exist) with absence assertions (invariant 3/6: halt_reason enum set patterns + exec keyword count both == 0) to catch regression from both directions. A missing polling block fails 7; a re-added polling block in SKILL.md fails invariant 1 baseline (9-match would shift). No single-direction invariant suffices for Scenario B enforcement."
    - "Canonical automated verify block — PLAN.md's <automated> verify field chains 10 assertions in a single `test ... && ... && ...` expression for binary exit code, while <acceptance_criteria> enumerates human-readable per-check rows. Executor runs both: automated block proves the success_criteria, acceptance_criteria rows provide per-invariant observability in SUMMARY.md."
    - "Pre-existing baseline recognition — invariant 2 (SKILL.md read-target guard) expected 0 but measured 3 pre-existing (L120/L307 are info-hiding prohibition comments; L578 is a legit HALT-path `Read {workspace}/lessons.md` instruction that predates Phase 3.1). Phase 3.1 did not introduce these. Plan's `0` expectation is an interpretation oversight — the canonical <verify> automated block correctly omits this grep. Documented as Deviation (not Rule 1 bug) because SKILL.md content is untouched by Plan 07."

key-files:
  created:
    - .planning/phases/03.1-engine-invocation-topology-refactor/03.1-07-SUMMARY.md
  modified:
    - CLAUDE.md

key-decisions:
  - "CLAUDE.md bullet 1 replacement strategy = full paradigm paragraph, not bullet delete + short replacement. Future agents editing tas read CLAUDE.md as load-bearing rules; a short bullet would omit the WHY (harness bash_id reap on subagent return) and the evidence chain (FINDINGS-nohup-bg-pattern.md §7 + EXPERIMENT-3v2). The verbose bullet carries: (a) historical context for why run_in_background: true was ever correct, (b) why it's now wrong in subagent contexts, (c) the exact Bash invocation pattern, (d) the polling loop chunking constraint (19 × 30s < 10min cap), (e) delegation boundary (MetaAgent polls; MainOrchestrator does NOT), (f) protocol.md cross-reference."
  - "Bullet 3 (exec prohibition) uses empirical proof-of-failure idiom (`bash -c 'trap \"echo TRAP\" EXIT; exec echo hi' prints hi but NOT TRAP`) rather than a reference-only citation. Plan Review §Issue #1 made the same empirical argument — replicating the one-liner in CLAUDE.md means any future agent can self-verify in 5 seconds before considering reintroducing exec. The static grep `grep -c '^[[:space:]]*exec ' skills/tas/runtime/run-dialectic.sh == 0` serves as the machine-verifiable backstop, referenced directly in the bullet."
  - "Invariant 2 (SKILL.md read-target guard) pre-existing 3-line baseline NOT altered. Plan 07 did not touch SKILL.md — altering it to pass invariant 2 would exceed Plan 07's scope (CLAUDE.md Common Mistakes only) and would violate Plan 05 SUMMARY's 'byte-identical' Phase 2 handler claim. The canonical automated <verify> block correctly omits this grep. Documented in Deviations §1 for forensic trail."
  - "Task 2 is read-only verify (no file modifications) — matches Plan 07 Task 2 explicit contract `<files>CLAUDE.md</files>` (for context only) + `<action>` field states 'this Task is read-only'. No per-task commit for Task 2; the final metadata commit (SUMMARY + STATE + ROADMAP + REQUIREMENTS) captures invariant verification as evidence."
  - "7-invariant set + Scenario A residue check + protocol smoke + REQUIREMENTS.md traceability aggregates to 25+ individual pass/fail assertions. All green except invariant 2 (3 pre-existing hits documented as non-regression). The canonical `<verify>` automated block exits 0."

patterns-established:
  - "Phase 3.1 close ritual: CLAUDE.md Common Mistakes paradigm-shift bullets are the project-instructions-layer regression shield. The full Phase 3.1 guardrail stack now has 4 layers: (1) doc layer — references/engine-invocation-protocol.md Scenario B + Issue #1 exec prohibition (Plan 03); (2) code-prompt layer — meta.md step 7/8/8b polling block + nohup spawn (Plan 04); (3) orchestrator-guardrail layer — SKILL.md Phase 0b halt_reason enum freeze bullet (Plan 05); (4) project-instructions layer — CLAUDE.md Common Mistakes bullets (Plan 07). 4-layer stack means any one layer's drift is caught by at least 2 others."
  - "Invariant 4 canary suite aggregates 5 runs (Canary #5 + Canary #6 PART A + PART B + Canary #7 smoke + test_exit_trap.sh) under Plan 07's single acceptance umbrella. SKIP is explicit-with-reason (Canary #6 PART A + test_exit_trap.sh Test C both SKIP on macOS without coreutils); silent PASS is forbidden. Canary #7 Phase 2 (real-chain sed-copy integration) + invariant 6 static grep together close Plan Review §관측 1 integration gap even in SKIP paths."

requirements-completed: [TOPO-06]

# Metrics
duration: 2m57s
completed: 2026-04-21
---

# Phase 3.1 Plan 07: CLAUDE.md Common Mistakes + Final Regression Verification Summary

**Replaced the existing `Invoking run-dialectic.sh in the foreground` bullet in `CLAUDE.md` Common Mistakes with a full Phase 3.1 Scenario B paradigm explanation (nohup fire-and-forget + MetaAgent-owned local polling + MainOrchestrator non-polling contract), added 2 new anti-pattern bullets (omitting nohup/&/echo $!; exec inside run-dialectic.sh), and verified the 7-invariant regression shield green. TOPO-06 documentation-side complete; Plan Review Issue #1 (exec vs EXIT trap) and Issue #2 (ownership contradiction) both close structurally. Canonical automated verify block (10 chained assertions) exits 0. Phase 3.1 ready for `/gsd-verify-work 3.1` handoff.**

## Performance

- **Duration:** 2m57s
- **Started:** 2026-04-21T16:07:42Z
- **Completed:** 2026-04-21T16:10:39Z
- **Tasks:** 2 (Task 1 atomic commit + Task 2 read-only verify)
- **Files modified:** 1 (`CLAUDE.md`, +3 lines net — 1 replaced bullet becomes 3 bullets)

## Accomplishments

- **TOPO-06 complete (documentation side).** `CLAUDE.md` "Common Mistakes When Editing This Repo" section now has 3 updated/new bullets covering the Phase 3.1 engine invocation topology:
  - **Bullet 1 (replaced):** "Invoking `run-dialectic.sh` with `run_in_background: true` from a subagent" — explains harness `bash_id` reap on subagent return (Phase 3 close blocker, original halt `_workspace/quick/20260421_143000/`), mandates Scenario B (nohup+& spawn → local polling via 19 × 30s chunked Bash → classify → return `status: completed | halted`), cross-references `references/engine-invocation-protocol.md` and `FINDINGS-nohup-bg-pattern.md §7`.
  - **Bullet 2 (new):** "Omitting any of `nohup` / `&` / `echo $!`" — all three load-bearing (SIGHUP ignore / shell-level background / PID capture). Canary #7 Phase 1 (`simulate_subagent_orphan.py`) cited as regression guard. Adds polling-Bash run_in_background:false rule.
  - **Bullet 3 (new):** "Using `exec` inside `skills/tas/runtime/run-dialectic.sh`" (Plan Review Issue #1 close-out) — empirical proof-of-failure idiom + static grep `grep -c "^[[:space:]]*exec " skills/tas/runtime/run-dialectic.sh == 0` backstop + Canary #7 Phase 2 integration reference.
- **All pre-existing bullets preserved byte-identical.** Other 10 Common Mistakes bullets (Adding implementation details / Reading agent files / Convergence depend on round count / Bypassing dialectic.py / Bash claude -p / Copying agent instructions / Pruning lessons.md / Auto-resume daemons / Reading dialectic artifacts / Fixed retry cap) untouched. Section order preserved.
- **`ALWAYS use run_in_background: true` phrase purged to 0 matches.** The now-obsolete Phase 3 advice is completely gone from CLAUDE.md. Grep `grep -c 'ALWAYS use \`run_in_background: true\`' CLAUDE.md` == `0`.
- **Canonical automated verify block passes.** PLAN.md Task 2 `<verify><automated>` chains 10 assertions (info-hiding 9-baseline + meta.md run_in_background:true absent + protocol.md task-notification absent + Canary #7 smoke + test_exit_trap.sh 5-case + run-dialectic.sh exec count 0 + bash -n OK + meta.md polling block presence × 2 + halt_reason enum freeze 5-file sum 0). Single chained expression exits 0 — Task 2 success criterion met.
- **7-invariant set fully verified (6 of 7 PASS + 1 pre-existing-baseline).**
  - **Invariant 1** (SKILL.md info-hiding 9-baseline): 9 — PASS
  - **Invariant 2** (SKILL.md read-target guard): 3 pre-existing lines (L120/L307 prohibition comments; L578 legit HALT lessons.md read) — NOT a Phase 3.1 regression. Plan's expectation of `0` is an interpretation oversight; the canonical `<verify>` automated block correctly omits this grep. See Deviations §1.
  - **Invariant 3** (cross-file halt_reason enum freeze, 5 files): 0 total — PASS
  - **Invariant 4** (Canary suite): 5/5 exit 0 — PASS
    - Canary #5 `simulate_stdout_stall.py` → `PASS: canary #5 (Layer A sdk_session_hang)`
    - Canary #6 PART A `simulate_step_transition.sh` → `SKIP: canary #6 requires coreutils` (macOS without brew coreutils — explicit SKIP, not a regression)
    - Canary #6 PART B `simulate_step_transition_unit.py` → 16 tests OK
    - Canary #7 smoke (T=10s) → `PASS: canary #7 (subagent orphan survived 10s, PPID=1; real-chain integration: PASS)` (10.4s wall-clock)
    - `test_exit_trap.sh` 5-case → Test A/B/D/E PASS + Test C SKIP (coreutils); overall `PASS: test_exit_trap (5 cases — TOPO-01 + Issue #1 regression guard)`
  - **Invariant 6a** (run-dialectic.sh exec line count): 0 — PASS (Issue #1 close-out static guard)
  - **Invariant 6b** (run-dialectic.sh syntax): `bash -n` OK — PASS
  - **Invariant 7** (meta.md polling block presence): 5/5 greps all match — PASS
    - `for i in $(seq 1 19)` ✓
    - `engine.done` ✓
    - `engine.exit` ✓
    - `TAS_POLL_INTERVAL_SEC` ✓
    - `ENGINE_PID|engine_pid` ✓
- **Scenario A residue check clean.** `engine_launched` count in SKILL.md = 0, in meta.md = 0. `Return early with engine metadata` in meta.md = 0. Confirms Plan Review Issue #2 resolution is structurally enforced across both orchestrator and MetaAgent prompts.
- **Protocol contract smoke green.** engine-invocation-protocol.md: `task-notification` count = 0 (old paradigm removed), `run_in_background: false` count = 3 (>= 1 required). meta.md: `nohup bash ${SKILL_DIR}/runtime/run-dialectic` present, `run_in_background: true` = 0.
- **REQUIREMENTS.md traceability.** `grep -c "TOPO-0[1-6]\|VERIFY-TOPO-01" .planning/REQUIREMENTS.md` = 15 (>= 14 required; 7 §C.1 bullets + 7 traceability table rows + 1 coverage count reference).

## Commits

| Task | Hash | Type | Description |
|------|------|------|-------------|
| 1 | `85b1054` | docs | update CLAUDE.md Common Mistakes for Scenario B + exec ban |
| 2 | n/a  | verify-only | Phase 3.1 7-invariant regression suite (no file modifications) |
| final metadata | `c199c57` | docs | complete Phase 3.1 — SUMMARY.md + STATE.md + ROADMAP.md + REQUIREMENTS.md |

## Verification

All plan acceptance criteria passed (except invariant 2 which is a pre-existing non-Phase-3.1 baseline — see Deviations §1). The canonical automated `<verify>` block exits 0.

### Task 1 (CLAUDE.md edit) — 13/13 checks PASS

| Check | Expected | Actual | Status |
|-------|----------|--------|--------|
| `grep -q "Phase 3.1 paradigm"` | exit 0 | exit 0 | PASS |
| `grep -q "Scenario B"` | exit 0 | exit 0 | PASS |
| `grep -q "nohup bash run-dialectic"` | exit 0 | exit 0 | PASS |
| `grep -q "engine.done"` | exit 0 | exit 0 | PASS |
| `grep -q "simulate_subagent_orphan.py"` | exit 0 | exit 0 | PASS |
| `grep -qE "exec.*run-dialectic\|exec keyword"` | exit 0 | exit 0 | PASS |
| `! grep -q "ALWAYS use run_in_background: true"` | exit 0 | exit 0 | PASS |
| Bullet 1 header (`Invoking ... with run_in_background: true from a subagent`) | present | present | PASS |
| FINDINGS-nohup-bg-pattern cross-ref | present | present | PASS |
| engine-invocation-protocol.md cross-ref | present | present | PASS |
| Bullet 2 (`Omitting any of nohup/&/echo`) | present | present | PASS |
| Bullet 3 (`Using exec inside ... run-dialectic.sh`) | present | present | PASS |
| Static regression guard reference | present | present | PASS |
| `grep -c "ALWAYS use run_in_background: true"` | 0 | 0 | PASS |
| `Bypassing dialectic.py` preserved | exit 0 | exit 0 | PASS |
| `Reading dialectic artifacts from SKILL.md during resume` preserved | exit 0 | exit 0 | PASS |
| Common Mistakes bullet total (sed-counted from header to EOF) | 11 + 2 = 13 | 13 | PASS |

Note: plan's suggested `awk '/^## Common Mistakes/,/^## /'` counts 0 bullets because Common Mistakes is the LAST section (awk range stops at header line). Equivalent `sed -n '/^## Common Mistakes/,$p' | grep -c '^- \*\*'` returns 13 = 11 pre-edit + 2 new. Semantic intent (+2 bullets) met.

### Task 2 (regression invariants) — 6/7 PASS + 1 pre-existing baseline

| Invariant | Expected | Actual | Status |
|-----------|----------|--------|--------|
| 1. SKILL.md info-hiding `(dialogue\|round-\|deliverable\|lessons\|heartbeat)` count | 9 | 9 | PASS |
| 2. SKILL.md read-target guard co-occurrence count | 0 | 3 (pre-existing baseline) | NOTE |
| 3. halt_reason enum set pattern sum (5 files) | 0 | 0 | PASS |
| 4a. Canary #5 `simulate_stdout_stall.py` | exit 0 + PASS line | exit 0 + `PASS: canary #5 (Layer A sdk_session_hang)` | PASS |
| 4b. Canary #6 PART A `simulate_step_transition.sh` | exit 0 (PASS or SKIP) | exit 0 + SKIP (no coreutils) | PASS |
| 4c. Canary #6 PART B `simulate_step_transition_unit.py` | exit 0 | exit 0 + 16 tests OK | PASS |
| 4d. Canary #7 smoke (T=10s) | exit 0 + PASS line | exit 0 + `PASS: canary #7 (subagent orphan survived 10s, PPID=1; real-chain integration: PASS)` | PASS |
| 4e. `test_exit_trap.sh` | exit 0 + PASS line | exit 0 + `PASS: test_exit_trap (5 cases — TOPO-01 + Issue #1 regression guard)` | PASS |
| 6a. run-dialectic.sh `^exec ` line count | 0 | 0 | PASS |
| 6b. run-dialectic.sh `bash -n` syntax | exit 0 | exit 0 | PASS |
| 7a. meta.md `for i in $(seq 1 19)` grep | exit 0 | exit 0 | PASS |
| 7b. meta.md `engine.done` grep | exit 0 | exit 0 | PASS |
| 7c. meta.md `engine.exit` grep | exit 0 | exit 0 | PASS |
| 7d. meta.md `TAS_POLL_INTERVAL_SEC` grep | exit 0 | exit 0 | PASS |
| 7e. meta.md `ENGINE_PID\|engine_pid` grep | exit 0 | exit 0 | PASS |
| Scenario A residue: SKILL.md `engine_launched` | 0 | 0 | PASS |
| Scenario A residue: meta.md `engine_launched` | 0 | 0 | PASS |
| Scenario A residue: meta.md `Return early with engine metadata` | 0 | 0 | PASS |
| Protocol: `task-notification` count | 0 | 0 | PASS |
| Protocol: `run_in_background: false` count | >= 1 | 3 | PASS |
| meta.md nohup paradigm grep | exit 0 | exit 0 | PASS |
| meta.md `run_in_background: true` count | 0 | 0 | PASS |
| SKILL.md `run_in_background: true` count | 0 | 0 | PASS |
| REQUIREMENTS.md TOPO/VERIFY-TOPO count | >= 14 | 15 | PASS |
| **Canonical `<verify>` automated block (10 chained assertions)** | **exit 0** | **exit 0** | **PASS** |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Spec interpretation] Invariant 2 expected `0` but pre-existing SKILL.md baseline is `3`**

- **Found during:** Task 2 invariant 2 check (`grep -E "(Read|cat|tail).*\.(md|txt)" skills/tas/SKILL.md | grep -cE "(dialogue\.md|round-.*\.md|deliverable\.md|lessons\.md|heartbeat\.txt)"`)
- **Issue:** Plan 07 PLAN.md acceptance criterion for invariant 2 expects output `0`, but the grep returns `3` against the current SKILL.md. The 3 matches are all **pre-existing** (pre-Phase-3.1) and Plan 07 did not modify SKILL.md:
  - **L120** — `# Reading dialogue.md, round-*.md, deliverable.md, or lessons.md is an I-1 regression.` — this is an info-hiding *prohibition comment* inside the Phase 0b HTML-comment block. It describes what NOT to read, not a read instruction.
  - **L307** — `- Read dialectic artifacts (dialogue.md / round-*.md / deliverable.md / lessons.md — I-1 regression)` — this is a *prohibition bullet* in a "does NOT" list (also Phase 0b). Describes forbidden reads, not permitted reads.
  - **L578** — `Read \`{workspace}/lessons.md\` for blockers. Count retry attempts:` — this is a legitimate pre-Phase-3.1 HALT recovery instruction that directs the main orchestrator to read `lessons.md` when displaying HALT UI. It predates Phase 3.1 and was never touched by any Plan in Phase 3.1 (01-06 SUMMARY confirmations).
- **Fix:** Not a code fix. Two observations:
  - (a) The canonical **automated `<verify>` block** in PLAN.md explicitly does NOT include this grep — only invariant 1 (the broader 9-match count) is in the chained assertion. The chained block exits 0, which is the machine-verifiable success criterion. So the executor is proceeding on the automated verify signal.
  - (b) Invariant 2 is stated in `<acceptance_criteria>` as a per-invariant row but the plan's action prose describes invariant 2 as a "추가 guard, unchanged" — the intent being that Phase 3.1's SKILL.md edits (which were minimal: Plan 05 added 1 Phase 0b bullet) must not introduce *new* lines matching the pattern. The 3 pre-existing lines are not a Phase 3.1 regression. Plan 05 SUMMARY explicitly verified that the 9-count info-hiding baseline was preserved by the absence of new artifact literals in the Plan 05 bullet.
- **Files modified:** none. Would-be fix (deleting L578 or rewording L120/L307) is out of scope for Plan 07 (which specifies `<files>CLAUDE.md</files>` only) and would contradict Plan 05 SUMMARY's "HALT Reason Labels + Recovery Guidance byte-identical" invariant.
- **Commit:** n/a (documentation only)
- **Pattern:** Same phase-level maturity pattern as Plans 03 (1x) + 04 (5x) + 05 (1x) + 06 (1x) — now 9 total Rule 3 plan-verbatim-vs-reality auto-fixes across Phase 3.1 where plan acceptance syntax didn't fit the file's actual byte layout. Future phase executors: when an acceptance grep expects `0` against a broad pattern in a large existing file, spot-check baseline before committing to the expectation.
- **Future prevention:** Plan reviewers should spot-check read-target guard greps against the baseline file pre-plan. When a file has pre-existing pattern matches that predate the phase, acceptance should state "no NEW matches introduced" rather than a flat count expectation, or the grep should be narrowed (e.g., exclude lines within HTML comment blocks or `## Common Mistakes`-style prohibition sections).

### Out-of-scope deferred items

None. Plan 07 executed exactly as written for Task 1 (CLAUDE.md Common Mistakes update). Task 2 is verify-only — no deferrals.

## Known Stubs

None. CLAUDE.md bullets are self-contained declarative anti-pattern descriptions with file-path cross-references. All referenced files exist and are populated:
- `skills/tas/runtime/tests/simulate_subagent_orphan.py` — Plan 06 landed the full 291-line Phase 1+2 body
- `skills/tas/references/engine-invocation-protocol.md` — Plan 03 landed the full Scenario B rewrite
- `FINDINGS-nohup-bg-pattern.md` — pre-existing repo root file with EXPERIMENT-3v2 §7 evidence
- `skills/tas/runtime/run-dialectic.sh` — Plan 02 landed EXIT trap + exec removal

## Threat Flags

None. CLAUDE.md is project-level Claude instructions; the 3 new/updated bullets document anti-patterns that existed in the repo code but were not previously declared at the project instructions layer. No new network surface, no new auth path, no schema change. The bullets point to Phase 3.1 already-landed guardrails (Canary #7, invariant 6 static grep) rather than introducing new surface.

## TDD Gate Compliance

N/A — Plan 07 is `type: execute` (not `type: tdd`). The `docs(03.1-07)` commit does not require `test(...)` → `feat(...)` → `refactor(...)` gating. Task 2 verify-only produces no commit.

## Self-Check: PASSED

- FOUND: `CLAUDE.md` (modified, +3 lines net — 1 bullet replaced, 2 bullets added)
- FOUND: commit `85b1054` (`git log --oneline --all | grep -q "85b1054"`)
- FOUND: `.planning/phases/03.1-engine-invocation-topology-refactor/03.1-07-SUMMARY.md` (this file)
- FOUND: `skills/tas/runtime/tests/simulate_subagent_orphan.py` (Canary #7, Plan 06)
- FOUND: `skills/tas/runtime/tests/test_exit_trap.sh` (TOPO-01 + Issue #1 guard, Plan 02)
- FOUND: `skills/tas/runtime/run-dialectic.sh` (EXIT trap + 0 exec lines, Plan 02)
- FOUND: `skills/tas/agents/meta.md` (Scenario B polling block, Plan 04)
- FOUND: `skills/tas/SKILL.md` (Phase 0b halt_reason freeze bullet, Plan 05)
- FOUND: `skills/tas/references/engine-invocation-protocol.md` (Scenario B rewrite, Plan 03)
- FOUND: `FINDINGS-nohup-bg-pattern.md` (EXPERIMENT-3v2 §7)
- VERIFIED: canonical automated `<verify>` block exits 0
- VERIFIED: 7-invariant set + Scenario A residue + protocol smoke + REQUIREMENTS.md traceability all green (invariant 2 documented as pre-existing baseline, not a Phase 3.1 regression)
