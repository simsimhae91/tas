---
phase: 03-2-layer-hang-watchdog
plan: 06
type: execute
wave: 2
depends_on: [03-04]
files_modified:
  - skills/tas/agents/meta.md
autonomous: true
requirements: [WATCH-04]
requirements_addressed: [WATCH-04]
must_haves:
  truths:
    - "meta.md Phase 2d step 7 prose contains the note that Bash timeout wrapping lives inside run-dialectic.sh (Layer B) and Agent()/Bash() tools have no timeout parameter"
    - "meta.md Phase 2d step 8 references the classification table in 03-CONTEXT.md D-05 and reproduces the 7-row table for executor locality"
    - "meta.md Phase 2d step 8 includes the synthesized HALT JSON example for bash_wrapper_kill / step_transition_hang cases"
    - "meta.md Within-Iteration FAIL Handling lists the three Phase 3 halt_reason values (sdk_session_hang, step_transition_hang, bash_wrapper_kill) as valid halt_reason strings for checkpoint write"
    - "No existing text in meta.md outside these anchors is modified"
  artifacts:
    - path: "skills/tas/agents/meta.md"
      provides: "MetaAgent classification reference + halt_reason enum extension for Phase 3 watchdog"
      contains: "sdk_session_hang"
  key_links:
    - from: "MetaAgent Phase 2d step 8"
      to: "Bash wrapper exit code + last-line JSON"
      via: "CONTEXT D-05 classification table"
      pattern: "classification table.*D-05"
    - from: "Within-Iteration FAIL Handling"
      to: "checkpoint.py write with halt_reason"
      via: "Phase 3 watchdog values appended to the valid halt_reason list"
      pattern: "sdk_session_hang"
---

<objective>
Wire MetaAgent's Execute Phase 2d (steps 7-9 + Within-Iteration FAIL Handling) to the Phase 3 classification model:

1. Step 7 gets a 2-line prose note making explicit that `run-dialectic.sh` is the only timeout wrapper (Agent() and Bash() tools have no timeout parameter).
2. Step 8 appends the CONTEXT D-05 classification table (7 rows) reproduced inline for executor locality, plus a synthesized-HALT-JSON example for the Layer B paths.
3. Within-Iteration FAIL Handling (line ~559-565) extends the valid `halt_reason` enumeration to include the three Phase 3 values: `sdk_session_hang`, `step_transition_hang`, `bash_wrapper_kill`.

All changes are append-only / parenthetical-extension per PATTERNS §SP-5 "order-stable table append" and §M3. No existing rows or sentences are reordered.

Purpose: MetaAgent becomes a correct classifier of run-dialectic.sh outcomes so the exit 124/137 and exit 0 + no-JSON paths both route to the right halt_reason and surface `last_heartbeat` forensics to SKILL.md (Plan 07).

Output: meta.md grows by ~30-35 lines across 3 insertion points.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-2-layer-hang-watchdog/03-CONTEXT.md
@.planning/phases/03-2-layer-hang-watchdog/03-RESEARCH.md
@.planning/phases/03-2-layer-hang-watchdog/03-PATTERNS.md
@skills/tas/agents/meta.md
@CLAUDE.md

<interfaces>
Current meta.md step 7 (lines 493-501) — anchor for a 2-line comment insertion after the closing of the existing `Bash({...})` example block.

Current meta.md step 8 (lines 503-509) — anchor for appending a classification table reference + example JSON AFTER the existing verdict-shape list (lines 506-509).

Current meta.md Within-Iteration FAIL Handling (lines 546-565) — anchor for extending the parenthetical `(values: persistent_verify_failure, persistent_test_failure, or the engine-emitted halt_reason from the last JSON line)` enumeration.

D-05 classification table (CONTEXT D-05 + Research §3.6) — 7 rows:
| exit | last-line JSON | classification | halt_reason | watchdog_layer |
| 0 | verdict: ACCEPT/PASS/FAIL | normal | — | — |
| 0 | halted, halt_reason: sdk_session_hang | Layer A hit | sdk_session_hang | A |
| 0 | halted, other halt_reason | engine HALT | pass-through | null |
| non-zero | valid JSON | engine crash + partial info | pass-through or engine_crash | A or null |
| 124/137 | any | Layer B SIGTERM/SIGKILL | bash_wrapper_kill | B |
| non-zero (other) | JSON absent | engine crash | engine_crash | null |
| 0 | JSON absent or parse-fail | step-transition hang | step_transition_hang | B |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert Phase 3 note into meta.md step 7 (Bash timeout wrapping scope)</name>
  <files>skills/tas/agents/meta.md</files>
  <read_first>
    - skills/tas/agents/meta.md lines 485-501 (current step 7 body including the Bash({...}) example block)
    - .planning/phases/03-2-layer-hang-watchdog/03-CONTEXT.md D-05 (lines 185-221) — "Agent() timeout 의미 해석" paragraph
    - .planning/phases/03-2-layer-hang-watchdog/03-RESEARCH.md §3.6 lines 601-608
    - .planning/phases/03-2-layer-hang-watchdog/03-PATTERNS.md §M3 lines 221-234
  </read_first>
  <behavior>
    - A 5-line blockquote note is inserted between the end of the step-7 Bash({...}) example code fence (around line 501) and the start of step 8 (line 503)
    - The note explicitly states that Bash timeout wrapping lives inside run-dialectic.sh (Layer B), that Agent() / Bash() have no timeout parameter, and that WATCH-04 "timeout on each step Agent() call" is fulfilled by (a) run-dialectic.sh's Bash timeout + (b) step-config `query_timeout` (Layer A)
    - No existing step 7 line is modified
  </behavior>
  <action>
In `skills/tas/agents/meta.md`, locate step 7's closing triple-backtick for the `Bash({...})` example code fence (around current line 501, just before the blank line preceding step 8). Insert the following blockquote block AFTER the closing ``` and BEFORE step 8:

```
   > **Phase 3 watchdog note**: Bash `timeout(1)` wrapping is performed
   > **inside** `run-dialectic.sh` (see WATCH-03). Do NOT add another
   > `timeout` wrapper at the `Bash(...)` tool level — double wrapping
   > confuses exit-code classification. The `Agent()` tool itself has no
   > timeout parameter; Phase 3's "timeout on each step Agent() call"
   > requirement (WATCH-04) is fulfilled by (a) run-dialectic.sh's Bash
   > timeout + (b) step-config `query_timeout` passed to the engine's
   > `asyncio.timeout` (Layer A).
```

Preserve the indentation style of step 7's surrounding prose (3-space indent for list continuations inside the ordered-list item — match neighbouring content byte-for-byte).
  </action>
  <verify>
    <automated>grep -q 'Phase 3 watchdog note' skills/tas/agents/meta.md && grep -q 'Bash `timeout(1)` wrapping is performed' skills/tas/agents/meta.md && grep -q 'WATCH-04' skills/tas/agents/meta.md && grep -q 'asyncio.timeout` (Layer A)' skills/tas/agents/meta.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Phase 3 watchdog note' skills/tas/agents/meta.md` → exit 0
    - `grep -q 'Bash `timeout(1)` wrapping is performed' skills/tas/agents/meta.md` → exit 0
    - `grep -q 'Do NOT add another' skills/tas/agents/meta.md` → exit 0
    - `grep -q 'WATCH-04' skills/tas/agents/meta.md` → exit 0
    - `grep -q 'asyncio.timeout' skills/tas/agents/meta.md` → exit 0
    - `grep -c '^7\. \*\*Execute engine\*\*' skills/tas/agents/meta.md` → 1 (step 7 heading still exists exactly once, not duplicated)
    - `grep -c '^8\. \*\*Await completion\*\*' skills/tas/agents/meta.md` → 1 (step 8 heading still exists)
  </acceptance_criteria>
  <done>Blockquote note present between step 7 example and step 8; step headings unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append CONTEXT D-05 classification table + synthesized HALT JSON example to meta.md step 8</name>
  <files>skills/tas/agents/meta.md</files>
  <read_first>
    - skills/tas/agents/meta.md lines 503-512 (current step 8 body)
    - .planning/phases/03-2-layer-hang-watchdog/03-CONTEXT.md D-05 (lines 186-221) — full classification table + synthesized HALT JSON
    - .planning/phases/03-2-layer-hang-watchdog/03-RESEARCH.md §3.6 lines 612-657
    - .planning/phases/03-2-layer-hang-watchdog/03-PATTERNS.md §M3 lines 236-244
  </read_first>
  <behavior>
    - A new sub-block is appended to step 8 AFTER the existing 4-bullet verdict-shape list (lines 506-509) and BEFORE step 9 (line 511)
    - The sub-block contains: introductory sentence "Apply the exit-code × last-line-JSON classification table in 03-CONTEXT.md D-05 (reproduced here for executor locality):" + a 7-row markdown table + a fenced JSON example for synthesized HALT payload + a `Bash({ command: "cat {LOG_DIR}/heartbeat.txt", ... })` usage hint + the "if dialectic.py already emitted a halted JSON with last_heartbeat, that takes precedence" rule
    - No existing step 8 line is modified
  </behavior>
  <action>
In `skills/tas/agents/meta.md`, locate step 8's existing 4-bullet verdict list (the block ending with the `- HALT: {"status":"halted",...}` line, approximately line 509). Immediately after that bullet and BEFORE step 9's heading (line 511), insert the following block. Use 3-space indentation for content continuation of ordered-list item 8.

```
   **Classification table (CONTEXT D-05 — authoritative; reproduced here
   for executor locality):**

   | exit | last-line JSON | classification | `halt_reason` | `watchdog_layer` |
   |------|----------------|----------------|---------------|------------------|
   | 0 | verdict: `ACCEPT` / `PASS` / `FAIL` | normal completion | — | — |
   | 0 | `status: halted`, `halt_reason: sdk_session_hang` | Layer A hit | `sdk_session_hang` | `A` |
   | 0 | `status: halted`, other `halt_reason` | engine internal HALT | (pass through) | null |
   | non-zero | valid JSON | engine crash + partial info | (pass through, else `engine_crash`) | `A` or null |
   | **124 / 137** | (any) | **Layer B SIGTERM/SIGKILL** | **`bash_wrapper_kill`** | **`B`** |
   | non-zero (other) | JSON absent | engine crash | `engine_crash` | null |
   | **0** | **JSON absent or parse-fail** | **step-transition hang** | **`step_transition_hang`** | **`B`** |

   For `bash_wrapper_kill` or `step_transition_hang`, the engine did not
   emit a halted JSON (process was SIGKILLed or exited without reaching
   the stdout emit). Synthesize a HALT JSON locally:

   ```json
   {
     "status": "halted",
     "verdict": "HALT",
     "halt_reason": "<bash_wrapper_kill|step_transition_hang>",
     "watchdog_layer": "B",
     "wrapper_exit": <exit_code>,
     "last_heartbeat": <parsed-from-{LOG_DIR}/heartbeat.txt, else null>,
     "round": <from heartbeat.round_n, else null>,
     "speaker": <from heartbeat.speaker, else null>,
     "halted_at": "iteration-{i}/step-{S.id}",
     "deliverable_path": null
   }
   ```

   Read `{LOG_DIR}/heartbeat.txt` for forensics via:
   ```
   Bash({
     command: "cat {LOG_DIR}/heartbeat.txt",
     run_in_background: false,
     description: "Forensics: last heartbeat before Layer B kill"
   })
   ```
   File absence → `last_heartbeat: null` (not an error). If `dialectic.py`
   already emitted a halted JSON with `last_heartbeat` populated, that JSON
   takes precedence over MetaAgent's local `cat`.
```

Leave step 8's existing 4 bullets (Standard / Inverted PASS / Inverted FAIL / HALT) byte-identical before this block.
  </action>
  <verify>
    <automated>grep -q 'CONTEXT D-05 — authoritative' skills/tas/agents/meta.md && grep -q '03-CONTEXT.md D-05' skills/tas/agents/meta.md && [ "$(grep -cE 'sdk_session_hang|step_transition_hang|bash_wrapper_kill' skills/tas/agents/meta.md)" -ge 3 ] && grep -q 'cat {LOG_DIR}/heartbeat.txt' skills/tas/agents/meta.md && grep -q '"wrapper_exit":' skills/tas/agents/meta.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'CONTEXT D-05 — authoritative' skills/tas/agents/meta.md` → exit 0
    - `grep -q '03-CONTEXT.md D-05' skills/tas/agents/meta.md` → exit 0 (reference string matches 03-VALIDATION.md task `03-XX-05` grep)
    - `grep -cE 'sdk_session_hang|step_transition_hang|bash_wrapper_kill' skills/tas/agents/meta.md` → >= 3 (each value appears at least once — matches 03-VALIDATION.md `03-XX-05`)
    - `grep -q 'cat {LOG_DIR}/heartbeat.txt' skills/tas/agents/meta.md` → exit 0
    - `grep -q '"wrapper_exit":' skills/tas/agents/meta.md` → exit 0
    - `grep -q '| \*\*124 / 137\*\* ' skills/tas/agents/meta.md` → exit 0 (Layer B row present)
    - `grep -c '^9\. \*\*Read deliverable\*\*' skills/tas/agents/meta.md` → 1 (step 9 heading still exists exactly once)
    - `grep -c '^9\.5\. \*\*Update checkpoint' skills/tas/agents/meta.md` → 1 (step 9.5 still exists)
  </acceptance_criteria>
  <done>Classification table + synthesized JSON example + heartbeat forensics hint appended to step 8; step 9 / 9.5 unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend Within-Iteration FAIL Handling halt_reason enumeration with Phase 3 values</name>
  <files>skills/tas/agents/meta.md</files>
  <read_first>
    - skills/tas/agents/meta.md lines 546-565 (current Within-Iteration FAIL Handling block)
    - .planning/phases/03-2-layer-hang-watchdog/03-CONTEXT.md D-05 (lines 213-217) — checkpoint HALT write path
    - .planning/phases/03-2-layer-hang-watchdog/03-CONTEXT.md D-06 (lines 223-247) — halt_reason enum alignment
    - .planning/phases/03-2-layer-hang-watchdog/03-RESEARCH.md §3.6 lines 660-676
    - .planning/phases/03-2-layer-hang-watchdog/03-PATTERNS.md §M3 lines 246-252
  </read_first>
  <behavior>
    - The existing "Write halt checkpoint" bullet (current line 559-565) is extended: the parenthetical `(values: persistent_verify_failure, persistent_test_failure, or the engine-emitted halt_reason from the last JSON line)` grows to include the three Phase 3 watchdog values
    - The insertion is append-only — Phase 1/2 values stay in their current position, Phase 3 values are appended at end of the enumeration
    - A 3-line annotation explains the Phase 3 additions (per Research §3.6 lines 667-676)
  </behavior>
  <action>
In `skills/tas/agents/meta.md`, locate the existing "Write halt checkpoint" bullet inside the Within-Iteration FAIL Handling subsection (currently around lines 559-565). The existing body reads approximately:

```
- **Write halt checkpoint** (CHKPT-01 halted): before returning the HALT JSON to
  the orchestrator, call `checkpoint.py write` with `status: "halted"` and
  `halt_reason: "<reason>"` (values: `persistent_verify_failure`,
  `persistent_test_failure`, or the engine-emitted `halt_reason` from the last
  JSON line). `current_step` remains the ID of the failing step, `completed_steps[]`
  remains the pre-HALT list. `updated_at` is the HALT moment. This write lets
  Phase 2 resume surface the last known progress and the halt reason.
```

Replace the parenthetical `(values: persistent_verify_failure, persistent_test_failure, or the engine-emitted halt_reason from the last JSON line)` with the expanded version below. Preserve every other character in the bullet byte-identically:

```
(values: `persistent_verify_failure`, `persistent_test_failure`, or the
  engine-emitted `halt_reason` from the last JSON line — including
  **Phase 3 watchdog values**: `sdk_session_hang` (Layer A asyncio.timeout
  expired inside dialectic.py), `step_transition_hang` (Layer B exit 0 +
  last-line JSON absent), `bash_wrapper_kill` (Layer B exit 124/137 via
  coreutils timeout SIGTERM/SIGKILL) — see 03-CONTEXT.md D-06)
```

Do NOT touch the surrounding bullet text (`**Write halt checkpoint** (CHKPT-01 halted):`, `call checkpoint.py write...`, `current_step remains...`, etc.).
  </action>
  <verify>
    <automated>grep -q 'Phase 3 watchdog values' skills/tas/agents/meta.md && grep -q '03-CONTEXT.md D-06' skills/tas/agents/meta.md && grep -q 'sdk_session_hang` (Layer A asyncio.timeout' skills/tas/agents/meta.md && grep -q 'step_transition_hang` (Layer B exit 0' skills/tas/agents/meta.md && grep -q 'bash_wrapper_kill` (Layer B exit 124/137' skills/tas/agents/meta.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Phase 3 watchdog values' skills/tas/agents/meta.md` → exit 0
    - `grep -q 'sdk_session_hang` (Layer A asyncio.timeout' skills/tas/agents/meta.md` → exit 0
    - `grep -q 'step_transition_hang` (Layer B exit 0' skills/tas/agents/meta.md` → exit 0
    - `grep -q 'bash_wrapper_kill` (Layer B exit 124/137' skills/tas/agents/meta.md` → exit 0
    - `grep -q '03-CONTEXT.md D-06' skills/tas/agents/meta.md` → exit 0
    - `grep -c '^- \*\*Write halt checkpoint\*\*' skills/tas/agents/meta.md` → 1 (bullet still exists exactly once)
    - `grep -q 'persistent_verify_failure' skills/tas/agents/meta.md` → exit 0 (existing enum values preserved)
    - `grep -q 'persistent_test_failure' skills/tas/agents/meta.md` → exit 0
  </acceptance_criteria>
  <done>Within-Iteration halt_reason enum extended with 3 Phase 3 values; Phase 1/2 values preserved; bullet structure intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run-dialectic.sh exit code → MetaAgent | read from Bash tool notification |
| {LOG_DIR}/heartbeat.txt → MetaAgent forensics | read-only `cat` via Bash tool |
| halt JSON (synthesized) → checkpoint.py write | MetaAgent-authored payload persisted to checkpoint.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-03 | T — tampering | `cat {LOG_DIR}/heartbeat.txt` reads a file dialectic.py wrote atomically | mitigate | Plan 03 enforces `os.replace` atomicity; MetaAgent reads only after process termination → no race. |
| T-3-11 | R — repudiation via misclassification | exit 125 ("invalid time interval") routes to engine_crash | accept | Per §security_gate T-3-01: user-error surfacing; documented in Plan 07 Recovery Guidance. |
| T-3-12 | I — info hiding regression | SKILL.md must not start reading heartbeat.txt | mitigate | Plan 07 optionally extends canary #4 regex to include heartbeat.txt (PATTERNS A2). This plan only adds MetaAgent-side reads — no SKILL.md touch here. |

No HIGH-severity threats.
</threat_model>

<verification>
1. `grep -q '03-CONTEXT.md D-05' skills/tas/agents/meta.md` → exit 0 (matches 03-VALIDATION.md task `03-XX-05`)
2. `grep -cE 'sdk_session_hang|step_transition_hang|bash_wrapper_kill' skills/tas/agents/meta.md` → >= 3
3. Step 7 note + step 8 classification table + Within-Iteration enum extension all present
4. Existing step headings (7, 8, 9, 9.5) still exactly one occurrence each (no accidental duplication)
</verification>

<success_criteria>
- WATCH-04 fulfilled: MetaAgent has the classification rules inline, references CONTEXT D-05, and knows how to synthesize HALT JSON for Layer B cases
- halt_reason enum extended in Within-Iteration FAIL Handling → checkpoint.py writes Phase 3 values without new code (halt_reason is an open-string field in checkpoint.json, not a validated enum)
- Plan 07 SKILL.md rows can display the 3 new halt_reason values via the existing HALT display path
</success_criteria>

<output>
After completion, create `.planning/phases/03-2-layer-hang-watchdog/03-06-SUMMARY.md` documenting:
- meta.md step 7 note + step 8 classification table + Within-Iteration enum extension complete
- MetaAgent can now correctly classify all 7 outcome combinations from Bash wrapper exit × last-line JSON
- heartbeat.txt forensic read path wired via `cat` (Layer B) and dialectic.py's self-emit (Layer A)
</output>
