---
phase: 03-2-layer-hang-watchdog
plan: 07
type: execute
wave: 3
depends_on: [03-05, 03-06, 03-04]
files_modified:
  - skills/tas/SKILL.md
  - skills/tas/references/engine-invocation-protocol.md
  - .planning/research/ARCHITECTURE.md
autonomous: true
requirements: [WATCH-05]
requirements_addressed: [WATCH-05]
must_haves:
  truths:
    - "SKILL.md HALT Reason Labels table contains 3 new rows (sdk_session_hang, step_transition_hang, bash_wrapper_kill) immediately after the already_completed row and before the (other) catch-all"
    - "SKILL.md Recovery Guidance table contains 3 new rows with Korean recovery messages per CONTEXT D-06 for the same three halt_reason values, same insertion position"
    - "engine-invocation-protocol.md Failure classification table gains 2 new rows (exit 124/137 → bash_wrapper_kill; exit 0 + no JSON → step_transition_hang) appended after the last existing row"
    - ".planning/research/ARCHITECTURE.md §6.4 contains a 2026-04-21 blockquote annotation noting that the old names (sdk_session_timeout, bash_timeout_killed) have been renamed to the ROADMAP names"
    - "No existing row / heading / sentence in any of the three files is moved or rewritten (order-stable append per PATTERNS SP-5)"
  artifacts:
    - path: "skills/tas/SKILL.md"
      provides: "Phase 3 halt_reason labels + Korean recovery guidance"
      contains: "sdk_session_hang"
    - path: "skills/tas/references/engine-invocation-protocol.md"
      provides: "Phase 3 failure classification rows"
      contains: "bash_wrapper_kill"
    - path: ".planning/research/ARCHITECTURE.md"
      provides: "Snapshot annotation reconciling old enum names to ROADMAP names"
      contains: "sdk_session_hang"
  key_links:
    - from: "halt JSON `halt_reason` field"
      to: "SKILL.md Phase 3 HALT display to user"
      via: "halt_reason → Display label + Recovery message lookup"
      pattern: "sdk_session_hang.*SDK 세션 무응답"
    - from: "MetaAgent classification table (Plan 06)"
      to: "engine-invocation-protocol.md Failure classification"
      via: "Append 2 rows — executor and reference doc agree"
      pattern: "bash_wrapper_kill"
---

<objective>
Close Phase 3's user-facing and reference-documentation surface:

1. `skills/tas/SKILL.md` — append 3 rows to the HALT Reason Labels table (Korean display labels) and 3 rows to the Recovery Guidance table (Korean recovery messages from CONTEXT D-06), inserted immediately before the `(other)` catch-all in each table.
2. `skills/tas/references/engine-invocation-protocol.md` — append 2 rows to the Failure classification table covering exit 124/137 → `bash_wrapper_kill` and exit 0 + no JSON → `step_transition_hang`.
3. `.planning/research/ARCHITECTURE.md` §6.4 — add a single blockquote annotation at the TOP of §6.4 noting that the old enum names (`sdk_session_timeout`, `bash_timeout_killed`) have been renamed to ROADMAP-official names (`sdk_session_hang`, `bash_wrapper_kill`) and that `step_transition_hang` is new. The body of §6.4 is preserved as historical snapshot (per D-06 decision + PATTERNS Anti-pattern A5).

Purpose: the halted JSON emitted by dialectic.py (Plan 04) or synthesized by MetaAgent (Plan 06) now maps cleanly to user-facing Korean copy via SKILL.md, and documentation (engine-invocation-protocol.md + research snapshot) stays consistent with the live code/meta.md surface.

Output: SKILL.md +6 lines, engine-invocation-protocol.md +2 lines, research/ARCHITECTURE.md +3 lines (annotation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-2-layer-hang-watchdog/03-CONTEXT.md
@.planning/phases/03-2-layer-hang-watchdog/03-RESEARCH.md
@.planning/phases/03-2-layer-hang-watchdog/03-PATTERNS.md
@skills/tas/SKILL.md
@skills/tas/references/engine-invocation-protocol.md
@CLAUDE.md

<interfaces>
SKILL.md HALT Reason Labels table (current lines 588-605):
- header (lines 588-589)
- 15 existing data rows (lines 590-604), last being `| already_completed | Already completed (이미 완료됨) |`
- `| (other) | Use halt_reason as-is |` (line 605)

SKILL.md Recovery Guidance table (current lines 622-636):
- header (lines 622-623)
- existing rows (lines 624-635), last being `| already_completed | "이미 완료된 run입니다. 새 요청은 /tas." |`
- `| (other) | "Check lessons.md for details." |` (line 636)

engine-invocation-protocol.md Failure classification table (current lines 77-83):
- header (lines 77-78)
- 5 existing data rows (lines 79-83), last being `| Engine stuck / zombied | ... | Return halt_reason: engine_lost ... |`

research/ARCHITECTURE.md §6.4 — planner locates the §6.4 heading and inserts the annotation blockquote immediately after the heading line.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append 3 rows to SKILL.md HALT Reason Labels + Recovery Guidance tables</name>
  <files>skills/tas/SKILL.md</files>
  <read_first>
    - skills/tas/SKILL.md lines 586-636 (both tables + surrounding prose)
    - .planning/phases/03-2-layer-hang-watchdog/03-CONTEXT.md D-06 (lines 223-247) — exact row content
    - .planning/phases/03-2-layer-hang-watchdog/03-RESEARCH.md §3.7 (lines 678-698)
    - .planning/phases/03-2-layer-hang-watchdog/03-PATTERNS.md §M4 (lines 256-271) + §SP-5 order-stable append
  </read_first>
  <behavior>
    - In the HALT Reason Labels table: insert 3 new rows immediately after the `| already_completed | Already completed (이미 완료됨) |` row (current line 604) and BEFORE the `| (other) | ... |` row (current line 605)
    - In the Recovery Guidance table: insert 3 new rows immediately after the `| already_completed | "이미 완료된 run입니다. 새 요청은 /tas." |` row (current line 635) and BEFORE the `| (other) | "Check lessons.md for details." |` row (current line 636)
    - Existing rows in both tables are preserved byte-identically
    - `(other)` catch-all rows stay at the bottom of each table
    - Recovery message for `bash_wrapper_kill` references `TAS_WATCHDOG_TIMEOUT_SEC` explicitly (matches D-06 copy; user needs the env var name to self-service resolution)
  </behavior>
  <action>
Make two precise insertions in `skills/tas/SKILL.md`.

**(a) HALT Reason Labels table.** Locate the row `| already_completed | Already completed (이미 완료됨) |` (current line 604). Immediately BELOW this row and ABOVE the `| (other) | Use halt_reason as-is |` row (current line 605), insert these three rows (match the pipe-alignment style of the neighbouring rows):

```
| `sdk_session_hang`     | SDK session hang (SDK 세션 무응답) |
| `step_transition_hang` | Step transition hang (스텝 전환 중 무응답) |
| `bash_wrapper_kill`    | Watchdog forced termination (워치독 강제 종료) |
```

**(b) Recovery Guidance table.** Locate the row `| already_completed | "이미 완료된 run입니다. 새 요청은 /tas." |` (current line 635). Immediately BELOW this row and ABOVE the `| (other) | "Check lessons.md for details." |` row (current line 636), insert these three rows:

```
| `sdk_session_hang`     | "SDK 응답이 없어 엔진이 중단됐습니다. `/tas --resume`으로 재시도하거나, 긴 step이면 `TAS_WATCHDOG_TIMEOUT_SEC`을 늘리세요." |
| `step_transition_hang` | "엔진이 결과 없이 종료됐습니다. `/tas --resume`으로 재시도하세요. 반복되면 `/tas-workspace`로 로그를 확인하세요." |
| `bash_wrapper_kill`    | "Watchdog이 설정된 시간 내 응답 부재로 프로세스를 종료했습니다. 긴 step이면 `TAS_WATCHDOG_TIMEOUT_SEC`을 늘린 후 `/tas --resume`으로 재시도하세요." |
```

Note: the third recovery row intentionally avoids raw numeric `{wrapper_exit}` substitution (per CONTEXT D-06 Claude's Discretion — "raw 124/137 노출은 혼란") and uses the natural-language phrase "설정된 시간 내". SKILL.md's renderer does not attempt `{wrapper_exit}` substitution — the synthesized HALT JSON from Plan 06 carries the exit code for forensics but the user-facing message is intentionally generic.

Do NOT add / modify / remove any other line in SKILL.md — tables before/after these anchors, HALT Display sample, etc. stay byte-identical.

Do NOT add any Read-heartbeat reference to SKILL.md (PATTERNS Anti-pattern A2 preserved — heartbeat.txt is not in SKILL.md's allowed-read list).
  </action>
  <verify>
    <automated>[ "$(grep -cE 'sdk_session_hang|step_transition_hang|bash_wrapper_kill' skills/tas/SKILL.md)" -ge 6 ] && grep -q 'SDK 세션 무응답' skills/tas/SKILL.md && grep -q 'Step transition hang' skills/tas/SKILL.md && grep -q '워치독 강제 종료' skills/tas/SKILL.md && grep -q 'TAS_WATCHDOG_TIMEOUT_SEC' skills/tas/SKILL.md && ! grep -nE 'heartbeat\.txt' skills/tas/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'sdk_session_hang|step_transition_hang|bash_wrapper_kill' skills/tas/SKILL.md` → >= 6 (3 in Labels table + 3 in Recovery table — matches 03-VALIDATION.md task `03-XX-07`)
    - `grep -q 'SDK 세션 무응답' skills/tas/SKILL.md` → exit 0
    - `grep -q '워치독 강제 종료' skills/tas/SKILL.md` → exit 0
    - `grep -q 'TAS_WATCHDOG_TIMEOUT_SEC' skills/tas/SKILL.md` → exit 0 (env var name surfaced to user)
    - `grep -c 'Use `halt_reason` as-is' skills/tas/SKILL.md` → 1 ((other) row still exists exactly once in Labels table — not duplicated)
    - `grep -c 'Check lessons.md for details.' skills/tas/SKILL.md` → 1 ((other) row still exists exactly once in Recovery table)
    - `grep -nE 'heartbeat\.txt' skills/tas/SKILL.md` → exit 1 (zero matches — A2 info-hiding preserved)
    - `grep -q 'already_completed' skills/tas/SKILL.md` → exit 0 (pre-existing anchor rows preserved)
  </acceptance_criteria>
  <done>6 new rows inserted; all Korean copy matches CONTEXT D-06; existing rows preserved; heartbeat.txt not introduced to SKILL.md.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append 2 rows to engine-invocation-protocol.md Failure classification table</name>
  <files>skills/tas/references/engine-invocation-protocol.md</files>
  <read_first>
    - skills/tas/references/engine-invocation-protocol.md lines 75-84 (current Failure classification table + everything immediately after)
    - .planning/phases/03-2-layer-hang-watchdog/03-CONTEXT.md D-05 (lines 186-216) — classification contract
    - .planning/phases/03-2-layer-hang-watchdog/03-RESEARCH.md §2 T25 (line 148) + §5 Dimension 5
    - .planning/phases/03-2-layer-hang-watchdog/03-PATTERNS.md §M5 (lines 275-291)
  </read_first>
  <behavior>
    - Two new rows appended after the last existing row (`| Engine stuck / zombied | No notification, liveness probe exit 1 | Return halt_reason: engine_lost with output_file path for forensics |`, current line 83)
    - No existing row (header, first 5 data rows) is modified or reordered
    - Rows use the same 3-column format: Condition | Evidence | Action
    - References to `watchdog_layer: B` match Plan 06's classification table
  </behavior>
  <action>
In `skills/tas/references/engine-invocation-protocol.md`, locate the Failure classification table. The last existing row is:

```
| Engine stuck / zombied | No notification, liveness probe exit 1 | Return `halt_reason: engine_lost` with `output_file` path for forensics |
```

Immediately after that row and BEFORE the section break / next `## What NOT to do` heading (around line 84), append these two rows with the same pipe-alignment style:

```
| Bash wrapper killed engine | Notification exit 124 or 137 | Return `halt_reason: bash_wrapper_kill`, `watchdog_layer: B`, `wrapper_exit: <code>`; read `{LOG_DIR}/heartbeat.txt` for `last_heartbeat` |
| Engine exit 0 + last line not JSON | Notification exit 0, no parseable JSON tail | Return `halt_reason: step_transition_hang`, `watchdog_layer: B`; read `{LOG_DIR}/heartbeat.txt` for `last_heartbeat` |
```

Do NOT modify the table header, the 5 existing data rows, the `## Why this protocol exists`, `## Standard invocation pattern`, `## Completion handling`, `## Liveness probe`, or `## What NOT to do` sections. This is an append-only edit between the last data row and the next section heading.
  </action>
  <verify>
    <automated>grep -q 'Bash wrapper killed engine' skills/tas/references/engine-invocation-protocol.md && grep -q 'Engine exit 0 + last line not JSON' skills/tas/references/engine-invocation-protocol.md && grep -q 'bash_wrapper_kill' skills/tas/references/engine-invocation-protocol.md && grep -q 'step_transition_hang' skills/tas/references/engine-invocation-protocol.md && grep -qE '124 or 137' skills/tas/references/engine-invocation-protocol.md && grep -q 'Engine stuck / zombied' skills/tas/references/engine-invocation-protocol.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Bash wrapper killed engine' skills/tas/references/engine-invocation-protocol.md` → exit 0
    - `grep -q 'Engine exit 0 + last line not JSON' skills/tas/references/engine-invocation-protocol.md` → exit 0
    - `grep -q 'bash_wrapper_kill' skills/tas/references/engine-invocation-protocol.md` → exit 0 (matches 03-VALIDATION.md task `03-XX-08`)
    - `grep -q 'step_transition_hang' skills/tas/references/engine-invocation-protocol.md` → exit 0
    - `grep -qE '124 or 137' skills/tas/references/engine-invocation-protocol.md` → exit 0 (matches 03-VALIDATION.md `03-XX-08`)
    - `grep -q 'Engine stuck / zombied' skills/tas/references/engine-invocation-protocol.md` → exit 0 (pre-existing row preserved)
    - `grep -q '## Why this protocol exists' skills/tas/references/engine-invocation-protocol.md` → exit 0 (section heading preserved)
    - `grep -q '## What NOT to do' skills/tas/references/engine-invocation-protocol.md` → exit 0 (next section heading preserved)
    - `wc -l skills/tas/references/engine-invocation-protocol.md | awk '{print $1}'` returns a value of at least 96 (original 94 + 2 appended rows; blank lines may shift result slightly higher)
  </acceptance_criteria>
  <done>2 new rows appended to the Failure classification table; surrounding sections byte-identical.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add snapshot annotation to research/ARCHITECTURE.md §6.4</name>
  <files>.planning/research/ARCHITECTURE.md</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md — locate §6.4 (grep for `## 6.4` or `### 6.4` depending on heading style; Research §9 places it around line 840)
    - .planning/phases/03-2-layer-hang-watchdog/03-CONTEXT.md D-06 (lines 229-231) — snapshot policy decision
    - .planning/phases/03-2-layer-hang-watchdog/03-RESEARCH.md §2 T30 (line 153) + §6 P3-06 (lines 1400-1413)
    - .planning/phases/03-2-layer-hang-watchdog/03-PATTERNS.md §M7 (lines 326-329) + Anti-pattern A5 (lines 454-456)
  </read_first>
  <behavior>
    - A single 3-line blockquote annotation is added at the TOP of §6.4, immediately after the §6.4 heading line and before the existing body text
    - The annotation explicitly notes the rename: `sdk_session_timeout` → `sdk_session_hang`, `bash_timeout_killed` → `bash_wrapper_kill`, plus the new `step_transition_hang`
    - The annotation references CONTEXT D-06 as authoritative
    - The body of §6.4 (the old-name table / prose) is preserved byte-identically (historical snapshot per PATTERNS A5)
  </behavior>
  <action>
In `.planning/research/ARCHITECTURE.md`, locate the `§6.4` heading line (it is a `###` or `##` heading containing "6.4" and likely "halt_reason" or similar). Immediately after that heading line and BEFORE the first body paragraph/table of the section, insert the following blockquote annotation block (one blank line before and after the blockquote, to isolate it from the body):

```
> **2026-04-21 update (Phase 3 execution)**: `halt_reason` names were realigned
> to ROADMAP-official values: `sdk_session_timeout` → `sdk_session_hang`,
> `bash_timeout_killed` → `bash_wrapper_kill`. Additionally,
> `step_transition_hang` is a new enum (Bash wrapper exit 0 + last-line JSON
> absent). The body below is preserved as a research snapshot; code and
> `meta.md` enum use the ROADMAP names. Authoritative source:
> `.planning/phases/03-2-layer-hang-watchdog/03-CONTEXT.md` D-06 + SKILL.md
> HALT Reason Labels.
```

Do NOT rewrite, rename, or remove any existing body text / table / bullet in §6.4. The old names remain verbatim as the snapshot.
  </action>
  <verify>
    <automated>grep -q '2026-04-21 update (Phase 3 execution)' .planning/research/ARCHITECTURE.md && grep -q 'sdk_session_timeout.*→.*sdk_session_hang' .planning/research/ARCHITECTURE.md && grep -q 'bash_timeout_killed.*→.*bash_wrapper_kill' .planning/research/ARCHITECTURE.md && grep -q 'step_transition_hang' .planning/research/ARCHITECTURE.md && grep -q '03-CONTEXT.md' .planning/research/ARCHITECTURE.md && grep -q '03-CONTEXT.md' .planning/research/ARCHITECTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '2026-04-21 update (Phase 3 execution)' .planning/research/ARCHITECTURE.md` → exit 0
    - `grep -q 'sdk_session_timeout.*→.*sdk_session_hang' .planning/research/ARCHITECTURE.md` → exit 0 (matches 03-VALIDATION.md task `03-XX-09`)
    - `grep -q 'bash_timeout_killed.*→.*bash_wrapper_kill' .planning/research/ARCHITECTURE.md` → exit 0
    - `grep -q 'step_transition_hang' .planning/research/ARCHITECTURE.md` → exit 0
    - `grep -q '03-CONTEXT.md' .planning/research/ARCHITECTURE.md` → exit 0
    - `grep -q 'sdk_session_timeout' .planning/research/ARCHITECTURE.md` → exit 0 (old name preserved in body as snapshot)
    - `grep -q 'bash_timeout_killed' .planning/research/ARCHITECTURE.md` → exit 0 (old name preserved in body as snapshot)
  </acceptance_criteria>
  <done>Annotation at top of §6.4; body preserved byte-identically with both old names still present as historical snapshot.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wire real canary bodies into Wave 0 stubs + register Canary #5 and #6 in canaries.md</name>
  <files>skills/tas/runtime/tests/simulate_stdout_stall.py, skills/tas/runtime/tests/simulate_step_transition.sh, skills/tas/runtime/tests/simulate_step_transition_unit.py, skills/tas-verify/canaries.md</files>
  <read_first>
    - skills/tas/runtime/tests/simulate_stdout_stall.py (Wave 0 stub from Plan 01)
    - skills/tas/runtime/tests/simulate_step_transition.sh (Wave 0 stub from Plan 01)
    - skills/tas/runtime/tests/simulate_step_transition_unit.py (Wave 0 stub from Plan 01)
    - skills/tas-verify/canaries.md — current file (143 lines, Canary #4 ends line 135)
    - .planning/phases/03-2-layer-hang-watchdog/03-CONTEXT.md D-07 (lines 249-272)
    - .planning/phases/03-2-layer-hang-watchdog/03-RESEARCH.md §3.8 (lines 700-891) — full Canary #5 body
    - .planning/phases/03-2-layer-hang-watchdog/03-RESEARCH.md §3.9 (lines 893-1012) — full Canary #6 body
    - .planning/phases/03-2-layer-hang-watchdog/03-RESEARCH.md §3.10 (lines 1014-1104) — full unit-test body
    - .planning/phases/03-2-layer-hang-watchdog/03-PATTERNS.md §NEW-1/2/3 + §M6 (lines 296-322)
    - skills/tas-verify/canaries.md Canary #4 (lines 100-135) — pattern template
    - skills/tas/runtime/dialectic.py (Plans 02/03/04 complete — real Layer A + heartbeat + halt emit available)
    - skills/tas/runtime/run-dialectic.sh (Plan 05 complete — Layer B wrapper available)
  </read_first>
  <behavior>
    - `simulate_stdout_stall.py` body is replaced by the full Research §3.8 canary (PYTHONPATH injection of mock SDK + subprocess-spawned dialectic.py + asserts halted JSON shape). Script exits 0 on PASS, 1 on FAIL. Target wall-clock ≤ 10s.
    - `simulate_step_transition.sh` body is replaced by the full Research §3.9 canary (export `TAS_WATCHDOG_TIMEOUT_SEC=5`, invoke `run-dialectic.sh` with mock config, assert exit 124 or 137). Preflight `command -v timeout || command -v gtimeout` → SKIP (exit 0) on coreutils-absent hosts (per D-03 graceful degrade policy).
    - `simulate_step_transition_unit.py` body replaces the Wave 0 skipped test with the full Research §3.10 body: a pure-Python `classify()` function mirroring CONTEXT D-05 classification table + 8 `unittest.TestCase` methods exercising all rows.
    - `skills/tas-verify/canaries.md` gains two new `## Canary #5` and `## Canary #6` sections inserted BETWEEN line 135 (end of Canary #4) and line 137 (`## When to add a new canary` heading). Each new section carries four sub-blocks matching Canary #4's template: `**Guards**`, `**Exercise**` (shell/python block), `**Pass criteria**`, `**Fail signals (regression)**`.
    - PATTERNS A3 preserved: all 3 test files stay stdlib-only (subprocess/unittest/pathlib/json/os/sys/tempfile/shutil/asyncio only).
    - PATTERNS A2 decision: extend Canary #4 grep regex with `|heartbeat\.txt` so the info-hiding lint also protects the Phase 3 new file. Update the `## Canary #4` Exercise block grep line accordingly (this is the one existing-section edit; the task specifies the exact new regex).
  </behavior>
  <action>
Perform four file replacements + one surgical regex edit + two file appends.

**(a) Replace `skills/tas/runtime/tests/simulate_stdout_stall.py`** with the complete body from Research §3.8 (lines 700-891 of `03-RESEARCH.md`). Copy the file verbatim — imports, `_MOCK_SHIM` constant, `_patch_dialectic_with_mock`, `main()`, `__main__` dispatch. Confirm the `REPO_ROOT = Path(__file__).resolve().parents[3]` expression yields the tas repo root (4 levels up from `skills/tas/runtime/tests/simulate_stdout_stall.py`: tests → runtime → tas → skills → <REPO_ROOT>). Confirm the mock client's `receive_response` stalls via `await asyncio.sleep(60)` with the dead-code `yield` marker. Confirm assertions check: `proc.returncode != 0`, last-line JSON `status == "halted"`, `halt_reason == "sdk_session_hang"`, `watchdog_layer == "A"`, `last_heartbeat` is a dict with all four required keys. On success emit `print("PASS: canary #5 (Layer A sdk_session_hang)")` and return 0; on failure print the specific FAIL reason and return 1.

**(b) Replace `skills/tas/runtime/tests/simulate_step_transition.sh`** with the complete body from Research §3.9 (lines 897-1010 of `03-RESEARCH.md`). Copy verbatim: `#!/bin/bash`, `set -u`, `REPO_ROOT` computation, `RUN_SCRIPT`/`DIALECTIC` path derivation, `command -v timeout || command -v gtimeout` preflight that echoes `SKIP:` and exits 0 on absence (per D-03 — NOT a regression), tmpdir creation via `mktemp -d`, mock claude_agent_sdk module generation, `step-config.json` generation, export of `TAS_WATCHDOG_TIMEOUT_SEC=5` and `TAS_WATCHDOG_KILL_GRACE_SEC=2`, timed invocation of `$RUN_SCRIPT`, exit-code assertion (must be 124 or 137 — anything else = FAIL), elapsed-time upper-bound assertion (≤ 17s).

**(c) Replace `skills/tas/runtime/tests/simulate_step_transition_unit.py`** with the complete body from Research §3.10 (lines 1018-1104 of `03-RESEARCH.md`). Copy verbatim: module docstring, `classify(exit_code, last_line) -> str` pure-Python mirror of meta.md D-05 table, `class StepTransitionHangTest(unittest.TestCase)` with at least 8 test methods (`test_exit0_json_absent`, `test_exit0_empty_line`, `test_exit0_parse_fail`, `test_exit0_valid_completed`, `test_exit124_bash_wrapper`, `test_exit137_bash_wrapper`, `test_exit0_halted_sdk_session_hang`, `test_exit1_engine_crash`). Keep the class name `StepTransitionHangTest` (Wave 0 grep token `class TestMetaAgentClassification` will NO LONGER match — so also add a 1-line alias `TestMetaAgentClassification = StepTransitionHangTest` after the class definition to preserve the Wave 0 grep-check compatibility; this alias is zero-cost).

**(d) Extend Canary #4 grep regex in `skills/tas-verify/canaries.md`** (PATTERNS A2 recommended, D-07 plan-stage decision: YES). Locate the existing regex line (current line 111):

```bash
grep -nE 'dialogue\.md|round-[0-9]+-(thesis|antithesis)\.md|deliverable\.md|lessons\.md' "$SKILL_PATH"
```

Replace it with:

```bash
grep -nE 'dialogue\.md|round-[0-9]+-(thesis|antithesis)\.md|deliverable\.md|lessons\.md|heartbeat\.txt' "$SKILL_PATH"
```

Only this single line changes in Canary #4's exercise block. The rest of Canary #4 stays byte-identical.

**(e) Append Canary #5 and Canary #6 sections to `skills/tas-verify/canaries.md`** between the end of Canary #4 (current line 135) and the start of `## When to add a new canary` (current line 137). Insert these two sections in the gap, each carrying the same sub-structure as Canary #4:

```markdown
## Canary #5 — Layer A stdout-stall (VERIFY-01 a)

**Guards**: `.planning/phases/03-2-layer-hang-watchdog/03-CONTEXT.md` D-07 Canary #5;
WATCH-01; WATCH-02 (last_heartbeat read path). Catches the regression class where
`dialectic.py`'s outer try/except fails to emit a halted JSON with
`halt_reason=sdk_session_hang` + `watchdog_layer=A` + 4-field `last_heartbeat` after
a TimeoutError fires inside `query_and_collect` (Layer A asyncio.timeout).

**Exercise**: subprocess-spawn `dialectic.py` with a PYTHONPATH-injected mock
ClaudeSDKClient whose `receive_response` stalls longer than `query_timeout`.
stdlib-only. Target wall-clock ≤ 10s.

```bash
python3 "${CLAUDE_PLUGIN_ROOT:-.}/skills/tas/runtime/tests/simulate_stdout_stall.py"
```

**Pass criteria**:
- Command exit code **0** and stdout contains the line `PASS: canary #5`
- Under the hood: spawned dialectic.py subprocess exit is non-zero (TimeoutError re-raise
  surfaces as sys.exit(1) in `__main__`)
- Last line of spawned dialectic.py's captured stdout parses as JSON with
  `status == "halted"`, `halt_reason == "sdk_session_hang"`, `watchdog_layer == "A"`,
  and a 4-field `last_heartbeat` object (timestamp, round_n, speaker, phase)

**Fail signals (regression)**:
- Exit code 1 with any `FAIL:` line → dialectic.py's Layer A halt emit regressed
  (missing field, wrong halt_reason, or no halted JSON at all)
- Wall-clock > 30s → Layer A may not be tripping; check that `_sdk_timeout` branch
  still uses `asyncio.timeout` on 3.11+ / `asyncio.wait_for` on 3.10

---

## Canary #6 — Layer B step-transition hang (VERIFY-01 b)

**Guards**: `.planning/phases/03-2-layer-hang-watchdog/03-CONTEXT.md` D-07 Canary #6;
WATCH-03; WATCH-04 (classification path). Catches regressions in the Bash watchdog
wrapper (`run-dialectic.sh` missing `--kill-after` or env var detection broken) and
in MetaAgent's classification of exit 0 + no-JSON → `step_transition_hang`.

**Exercise**: two parts. PART A drives Layer B SIGTERM/SIGKILL end-to-end (requires
coreutils `timeout` or `gtimeout` — SKIP on absent hosts per D-03 graceful degrade).
PART B unit-tests the classification function that mirrors meta.md D-05.

```bash
# PART A — end-to-end Layer B kill (SKIP on no-coreutils macOS)
bash "${CLAUDE_PLUGIN_ROOT:-.}/skills/tas/runtime/tests/simulate_step_transition.sh"

# PART B — classification table unit mirror
python3 "${CLAUDE_PLUGIN_ROOT:-.}/skills/tas/runtime/tests/simulate_step_transition_unit.py"
```

**Pass criteria**:
- PART A exit 0 with stdout `PASS: canary #6` OR `SKIP:` (SKIP only on hosts without
  `timeout`/`gtimeout` — SKIP is explicitly NOT a regression per D-03)
- PART A wall-clock ≤ 17s (TIMEOUT_SEC=5 + KILL_GRACE=2 + ~10s startup margin)
- Spawned `run-dialectic.sh` exit code must be 124 (SIGTERM) or 137 (SIGKILL)
- PART B: all 8 `unittest` test methods pass (exit 0 from `python3 ...simulate_step_transition_unit.py`)

**Fail signals (regression)**:
- PART A exit 1 with `FAIL:` line → Layer B wrapper or `--kill-after` broke; check
  `run-dialectic.sh` lines that detect `timeout`/`gtimeout` and the wrapped-exec
  branch
- PART A exit code not in {0, 124, 137} with coreutils installed → preflight
  regression (check `command -v` idiom)
- PART B unittest failure → MetaAgent classification drift (03-CONTEXT.md D-05);
  reconcile with the meta.md table the canary mirrors
```

Preserve the `---` separator before `## When to add a new canary` and keep that section byte-identical.
  </action>
  <verify>
    <automated>python3 skills/tas/runtime/tests/simulate_stdout_stall.py 2>&1 | tee /tmp/c5.log; EC5=$?; bash skills/tas/runtime/tests/simulate_step_transition.sh 2>&1 | tee /tmp/c6a.log; EC6=$?; python3 skills/tas/runtime/tests/simulate_step_transition_unit.py 2>&1 | tee /tmp/c6u.log; EC6U=$?; grep -q '^## Canary #5' skills/tas-verify/canaries.md && grep -q '^## Canary #6' skills/tas-verify/canaries.md && grep -q 'heartbeat\\.txt' skills/tas-verify/canaries.md && [ "$EC5" -eq 0 ] && [ "$EC6" -eq 0 ] && [ "$EC6U" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `python3 skills/tas/runtime/tests/simulate_stdout_stall.py` → exit 0 (Canary #5 PASS — or reports its own FAIL with non-zero exit if Layer A regressed)
    - `bash skills/tas/runtime/tests/simulate_step_transition.sh` → exit 0 (PASS with 124/137 exit from wrapper; OR SKIP on no-coreutils hosts per D-03)
    - `python3 skills/tas/runtime/tests/simulate_step_transition_unit.py` → exit 0 (all 8 tests pass)
    - `grep -q '^## Canary #5 — Layer A stdout-stall (VERIFY-01 a)' skills/tas-verify/canaries.md` → exit 0
    - `grep -q '^## Canary #6 — Layer B step-transition hang (VERIFY-01 b)' skills/tas-verify/canaries.md` → exit 0
    - `grep -q 'heartbeat\.txt' skills/tas-verify/canaries.md` → exit 0 (Canary #4 regex extended per A2)
    - `grep -q '^## When to add a new canary' skills/tas-verify/canaries.md` → exit 0 (final section preserved)
    - `grep -q 'class StepTransitionHangTest' skills/tas/runtime/tests/simulate_step_transition_unit.py` → exit 0
    - `grep -q 'TestMetaAgentClassification = StepTransitionHangTest' skills/tas/runtime/tests/simulate_step_transition_unit.py` → exit 0 (Wave 0 grep compatibility alias)
    - `grep -q 'PASS: canary #5' skills/tas/runtime/tests/simulate_stdout_stall.py` → exit 0
    - `grep -q 'TAS_WATCHDOG_TIMEOUT_SEC=5' skills/tas/runtime/tests/simulate_step_transition.sh` → exit 0
    - `grep -qE 'timeout >/dev/null 2>&1' skills/tas/runtime/tests/simulate_step_transition.sh` → exit 0 (preflight present)
    - `grep -E '^(import|from) (pytest|async_timeout|psutil)' skills/tas/runtime/tests/simulate_stdout_stall.py skills/tas/runtime/tests/simulate_step_transition_unit.py` → exit 1 (A3 preserved)
  </acceptance_criteria>
  <done>All 3 canary files replaced with full Research §3.8/3.9/3.10 bodies; Canary #4 regex extended with heartbeat.txt; Canary #5 + #6 sections registered in canaries.md; full suite (5 + 6a + 6b) exits 0 on current dev machine (6a via SKIP on no-coreutils host).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| halt JSON → SKILL.md user display | halt_reason value maps to Korean label + recovery message via static table lookup |
| `cat {LOG_DIR}/heartbeat.txt` (MetaAgent only) → Canary #4 info-hiding lint | regex extended with `heartbeat\.txt` so SKILL.md can never add this read |
| canary subprocess → test tmpdir | `mktemp -d` + `trap rm -rf` cleanup on exit |
| research/ARCHITECTURE.md → downstream agents | annotation makes rename authoritative without touching body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-04 | I — info disclosure | halt JSON `workspace` field shown in SKILL.md display | accept | Per §security_gate T-3-04: local CLI only; no network; no mitigation required for M1. SKILL.md Recovery Guidance rows avoid surfacing workspace path directly. |
| T-3-13 | T — tampering via regex drift | Canary #4 regex missing new file types | mitigate | A2 decision: extend regex with `heartbeat\.txt` this plan; future new artefact files (Phase 4 chunks, Phase 5 references) will be added via same pattern. |
| T-3-14 | R — repudiation via research/impl drift | old enum names in ARCHITECTURE.md §6.4 could be resurrected | mitigate | A5 + P3-06: annotation at top of §6.4 explicitly warns that code/meta.md use the new names; old names preserved as snapshot only. |
| T-3-15 | D — DoS via long-running canary | canary #6 PART A spawns engine | accept | Per §security_gate: wall-clock ≤ 17s; mktemp tmpdir auto-cleaned via trap; SKIP on no-coreutils hosts. |

No HIGH-severity threats.
</threat_model>

<verification>
Phase-wide structural check (run after all 7 plans complete):

1. `grep -cE 'sdk_session_hang|step_transition_hang|bash_wrapper_kill' skills/tas/SKILL.md` → >= 6
2. `grep -q '03-CONTEXT.md D-05' skills/tas/agents/meta.md` → exit 0
3. `grep -q 'asyncio.timeout' skills/tas/runtime/dialectic.py` → exit 0
4. `grep -c '_heartbeat(log_dir,' skills/tas/runtime/dialectic.py` → >= 8
5. `grep -qE 'command -v (timeout|gtimeout)' skills/tas/runtime/run-dialectic.sh` → exit 0
6. `grep -q -- '--kill-after' skills/tas/runtime/run-dialectic.sh` → exit 0
7. `grep -q 'bash_wrapper_kill' skills/tas/references/engine-invocation-protocol.md` → exit 0
8. `grep -q '2026-04-21 update (Phase 3 execution)' .planning/research/ARCHITECTURE.md` → exit 0
9. Full canary suite: all 3 scripts exit 0 (canary #6 PART A may SKIP on no-coreutils hosts)
10. `grep -nE 'heartbeat\.txt' skills/tas/SKILL.md` → exit 1 (info-hiding preserved — heartbeat.txt NOT read by SKILL.md)
</verification>

<success_criteria>
- WATCH-05 fulfilled: SKILL.md has 3 Phase 3 halt_reason rows in both tables; users see Korean labels + recovery guidance when watchdog fires
- VERIFY-01 (a) canary PASS: `simulate_stdout_stall.py` asserts Layer A halted JSON shape end-to-end
- VERIFY-01 (b) canary PASS: `simulate_step_transition.sh` trips Layer B (or SKIP on no-coreutils); classification unit mirror asserts all 8 D-05 rows
- engine-invocation-protocol.md + ARCHITECTURE.md §6.4 aligned with ROADMAP enum names
- Canary #4 regex extended with `heartbeat\.txt` so the info-hiding boundary now covers the Phase 3 agent-internal artefact
- /tas-verify suite (Canary #1-#6) ready to run as one atomic regression gate
</success_criteria>

<output>
After completion, create `.planning/phases/03-2-layer-hang-watchdog/03-07-SUMMARY.md` documenting:
- SKILL.md + engine-invocation-protocol.md + research/ARCHITECTURE.md edits complete (all order-stable appends)
- 3 Wave 0 stubs replaced with full Research §3.8 / §3.9 / §3.10 bodies
- Canaries #5 and #6 registered in canaries.md; Canary #4 regex extended with heartbeat.txt
- Full `/tas-verify` suite runnable; Phase 3 ready for gsd-verify-work
</output>
